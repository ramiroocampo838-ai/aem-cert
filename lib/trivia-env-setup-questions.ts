/**
 * Trivia — AEM Environment Setup Question Bank
 * 104 questions covering env-setup.txt topics (14 sections)
 * Each question: ≥4 correct answer variants, ≥8 incorrect distractors
 */

import type { TriviaQuestion, TriviaSectionConfig } from "./trivia-types"

export const ENV_SETUP_SECTION_CONFIG: TriviaSectionConfig = {
  id: "env-setup",
  label: "Environment Setup",
  description: "QuickStart Jar, Maven modules, Dispatcher, Repo Init, and local dev tools",
  icon: "Terminal",
  color: "blue",
  questionCount: 104,
}

export const envSetupQuestions: TriviaQuestion[] = [

  // ── DEVELOPMENT TOOLS (8) ────────────────────────────────────────────────────

  {
    id: "env-009",
    topic: "Development Tools",
    question: "Which Java version is recommended for local AEM development?",
    correctAnswers: [
      "Java JDK 21",
      "Java 21",
      "JDK 21 is the recommended Java version for AEM development",
      "Java Development Kit 21",
    ],
    incorrectAnswers: [
      "Java 8",
      "Java 11",
      "Java 17",
      "Java 22",
      "Java 16",
      "Java 1.8",
      "Java 13",
      "Java 20",
    ],
    explanation:
      "Java JDK 21 is the recommended version for AEM development. AEM is a Java application, so the correct JDK must be installed before starting local development.",
  },
  {
    id: "env-010",
    topic: "Development Tools",
    question: "What command verifies that Java is correctly installed on your system?",
    correctAnswers: [
      "java -version",
      "Running java -version in the terminal",
      "The java -version command",
      "java -version — which prints the installed JDK version",
    ],
    incorrectAnswers: [
      "java --version",
      "javac -check",
      "jdk -version",
      "java -v",
      "check-java",
      "java version",
      "java -info",
      "jvm -version",
    ],
    explanation:
      "After installing Java JDK, you verify the installation with the command `java -version`. This prints the installed JDK version to confirm it is correctly set up.",
  },
  {
    id: "env-011",
    topic: "Development Tools",
    question: "What is the build tool used for all AEM projects?",
    correctAnswers: [
      "Apache Maven",
      "Maven is the build tool for all AEM projects",
      "Apache Maven — the standard build tool for AEM development",
      "Maven (mvn) — used to build, package, and deploy AEM code",
    ],
    incorrectAnswers: [
      "Gradle",
      "npm",
      "Ant",
      "Webpack",
      "Vite",
      "Make",
      "Bazel",
      "Jenkins",
    ],
    explanation:
      "Apache Maven is the build tool for all AEM projects. It compiles Java code, runs tests, and packages the code into OSGi bundles and content packages for deployment.",
  },
  {
    id: "env-012",
    topic: "Development Tools",
    question: "What command verifies that Apache Maven is correctly installed?",
    correctAnswers: [
      "mvn -v",
      "Running mvn -v in the terminal",
      "The mvn -v command prints the Maven version",
      "mvn -v — which shows the Maven installation details",
    ],
    incorrectAnswers: [
      "maven -version",
      "mvn --version",
      "mvn -check",
      "mvn version",
      "apache-maven -v",
      "mvn -info",
      "build -v",
      "mvn status",
    ],
    explanation:
      "You verify Maven is installed correctly with `mvn -v`, which prints the Maven version, Java version, and OS information. This confirms the Maven installation is working.",
  },
  {
    id: "env-013",
    topic: "Development Tools",
    question: "Why is Node.js required in the AEM local development environment?",
    correctAnswers: [
      "For front-end tooling and the Adobe I/O CLI",
      "Node.js is needed for front-end tooling and running the Adobe I/O CLI (aio)",
      "Required to install and run front-end build tools and the aio CLI",
      "Needed for the Adobe I/O CLI and front-end asset compilation",
    ],
    incorrectAnswers: [
      "AEM is built on Node.js and requires it to run",
      "Node.js replaces Java as the AEM runtime engine",
      "Required to compile Java OSGi bundles",
      "Needed to start the QuickStart Jar",
      "Used to run the Apache Dispatcher locally",
      "Required for the AEM JCR repository to function",
      "Node.js is the build tool that replaces Maven",
      "Needed only for running unit tests in AEM",
    ],
    explanation:
      "Node.js is required for front-end tooling (like webpack, Sass compilation) and for the Adobe I/O CLI (`aio`), which is installed as a global npm package. It is not used to run AEM itself.",
  },
  {
    id: "env-014",
    topic: "Development Tools",
    question: "What command verifies that Node.js is correctly installed?",
    correctAnswers: [
      "node --version",
      "Running node --version in the terminal",
      "The node --version command",
      "node --version — which prints the installed Node.js version",
    ],
    incorrectAnswers: [
      "node -v",
      "nodejs --version",
      "node -version",
      "npm -version",
      "node --check",
      "node status",
      "node -info",
      "nvm version",
    ],
    explanation:
      "After installing Node.js, you verify it with `node --version`, which prints the installed Node.js version number. This confirms Node.js is available in your PATH.",
  },
  {
    id: "env-015",
    topic: "Development Tools",
    question: "Which of the following is one of the four core tools required for local AEM development?",
    correctAnswers: [
      "Git — for source control",
      "Git is one of the four required tools for AEM local development",
      "Git, used for source control of the AEM project code",
      "Git — one of four core tools alongside Java, Maven, and Node.js",
    ],
    incorrectAnswers: [
      "Docker Desktop",
      "IntelliJ IDEA",
      "Visual Studio Code",
      "Postman",
      "SonarQube",
      "Jenkins",
      "Kubernetes CLI (kubectl)",
      "Adobe Acrobat",
    ],
    explanation:
      "The four core tools required for local AEM development are: Java JDK 21, Node.js, Apache Maven, and Git. Git is used for source control of the AEM project.",
  },
  {
    id: "env-016",
    topic: "Development Tools",
    question: "What is the recommended practice after installing each development tool for AEM?",
    correctAnswers: [
      "Verify the installation with a version command",
      "Always verify each tool with a version command after installing",
      "Run a version check command to confirm the tool is correctly installed",
      "Confirm installation by running the tool's version verification command",
    ],
    incorrectAnswers: [
      "Restart the machine and then start the QuickStart Jar",
      "Run a full Maven build immediately to validate the setup",
      "Open CRXDE Lite to confirm the tool is recognized by AEM",
      "Check the Adobe Developer Console for tool registration",
      "Deploy a test package to Cloud Manager to verify the tool",
      "Run the Adobe I/O CLI diagnostic command",
      "Launch the QuickStart Jar in debug mode",
      "Check the OSGi Bundle Console for the installed version",
    ],
    explanation:
      "After installing each tool (Java, Maven, Node.js, Git), you should always verify with a version command: `java -version`, `mvn -v`, `node --version`, and `git --version`. This confirms the tool is installed and on the PATH.",
  },

  // ── ADOBE I/O CLI (8) ────────────────────────────────────────────────────────

  {
    id: "env-017",
    topic: "Adobe I/O CLI",
    question: "What is the Adobe I/O CLI and how is it commonly referred to?",
    correctAnswers: [
      "A command-line tool for interacting with Adobe services, known as aio",
      "The aio CLI — a command-line tool to interact with Adobe cloud services",
      "Adobe I/O CLI (aio) — lets you interact with Adobe services from the terminal",
      "aio, the Adobe I/O command-line interface for managing Adobe cloud integrations",
    ],
    incorrectAnswers: [
      "A GUI tool for managing AEM instances, known as aem-admin",
      "A browser extension for Adobe Developer Console, known as aio",
      "A Maven plugin for deploying AEM packages, called aio-deploy",
      "A Node.js runtime bundled with AEM, called adobe-io",
      "The Adobe Identity Manager CLI for SSO configuration",
      "A Docker image that runs Adobe microservices locally",
      "The Cloud Manager CLI for triggering pipelines, called cm-cli",
      "An Eclipse plugin for connecting to AEM Author",
    ],
    explanation:
      "The Adobe I/O CLI, known as `aio`, is a command-line tool that lets you interact with Adobe services from the terminal. It is installed globally via npm and extended with plugins.",
  },
  {
    id: "env-018",
    topic: "Adobe I/O CLI",
    question: "What command installs the Adobe I/O CLI?",
    correctAnswers: [
      "npm install -g @adobe/aio-cli",
      "npm install -g @adobe/aio-cli — installs aio as a global npm package",
      "The command npm install -g @adobe/aio-cli installs the Adobe I/O CLI globally",
      "Install globally with: npm install -g @adobe/aio-cli",
    ],
    incorrectAnswers: [
      "npm install @adobe/aio-cli",
      "npm install -g adobe-cli",
      "npm install -g aio",
      "npm install -g @adobe/io-cli",
      "brew install adobe/aio",
      "mvn install adobe-io-cli",
      "pip install adobe-aio-cli",
      "apt-get install adobe-aio-cli",
    ],
    explanation:
      "The Adobe I/O CLI is installed as a global npm package with `npm install -g @adobe/aio-cli`. The `-g` flag installs it globally so the `aio` command is available in any terminal session.",
  },
  {
    id: "env-019",
    topic: "Adobe I/O CLI",
    question: "Which of the following is a plugin you can add to the Adobe I/O CLI for AEM development?",
    correctAnswers: [
      "Cloud Manager plugin",
      "The Cloud Manager plugin for the Adobe I/O CLI",
      "AEM Rapid Development Environment (RDE) plugin",
      "The Asset Compute plugin for the Adobe I/O CLI",
    ],
    incorrectAnswers: [
      "SonarQube plugin",
      "Maven Sling deploy plugin",
      "Apache Dispatcher plugin",
      "Oak Index manager plugin",
      "Docker Compose plugin",
      "Jenkins pipeline plugin",
      "GitHub Actions plugin",
      "AEM Package Manager plugin",
    ],
    explanation:
      "Three key plugins for AEM development are: the Cloud Manager plugin, the AEM Rapid Development Environment (RDE) plugin, and the Asset Compute plugin. These extend the `aio` CLI with Adobe service capabilities.",
  },
  {
    id: "env-020",
    topic: "Adobe I/O CLI",
    question: "Which authentication method was removed from the Adobe I/O CLI in January 2025?",
    correctAnswers: [
      "JWT authentication",
      "JWT (JSON Web Token) authentication was removed in January 2025",
      "Service Account (JWT) authentication — removed in January 2025",
      "The JWT-based authentication method was discontinued in January 2025",
    ],
    incorrectAnswers: [
      "OAuth Server-to-Server authentication",
      "Basic authentication with username and password",
      "API Key authentication",
      "SAML 2.0 authentication",
      "OAuth 2.0 PKCE authentication",
      "IMS Bearer token authentication",
      "mTLS certificate-based authentication",
      "OpenID Connect authentication",
    ],
    explanation:
      "JWT (JSON Web Token) authentication was removed from the Adobe I/O CLI in January 2025. It has been replaced by OAuth Server-to-Server authentication configured through the Adobe Developer Console.",
  },
  {
    id: "env-021",
    topic: "Adobe I/O CLI",
    question: "What is the only supported authentication method for the Adobe I/O CLI after January 2025?",
    correctAnswers: [
      "OAuth Server-to-Server authentication",
      "OAuth Server-to-Server — the only supported method since January 2025",
      "OAuth Server-to-Server, configured through the Adobe Developer Console",
      "Server-to-Server OAuth credentials via the Adobe Developer Console",
    ],
    incorrectAnswers: [
      "JWT Service Account authentication",
      "Basic HTTP authentication",
      "API Key with secret",
      "SAML assertion",
      "Client certificate (mTLS)",
      "IMS refresh token",
      "OpenID Connect implicit flow",
      "OAuth 2.0 authorization code flow",
    ],
    explanation:
      "After January 2025, OAuth Server-to-Server is the only supported authentication method. It is configured through the Adobe Developer Console, replacing the deprecated JWT Service Account approach.",
  },
  {
    id: "env-022",
    topic: "Adobe I/O CLI",
    question: "Where is OAuth Server-to-Server authentication for the Adobe I/O CLI configured?",
    correctAnswers: [
      "In the Adobe Developer Console",
      "Through the Adobe Developer Console project",
      "The Adobe Developer Console is where OAuth Server-to-Server credentials are configured",
      "Adobe Developer Console — where you set up the Server-to-Server OAuth integration",
    ],
    incorrectAnswers: [
      "In the Cloud Manager environment variables",
      "In the AEM Author Admin Console",
      "In the aio CLI configuration file (~/.aio)",
      "In the Adobe Admin Console user settings",
      "In the AEM OSGi configuration console",
      "In the Maven settings.xml file",
      "In the AEM Package Manager",
      "Directly in the environment's .env file",
    ],
    explanation:
      "OAuth Server-to-Server credentials are configured in the Adobe Developer Console. You create a project there, add the required APIs and services, and generate the credentials used by the `aio` CLI.",
  },
  {
    id: "env-023",
    topic: "Adobe I/O CLI",
    question: "What is the purpose of the AEM Rapid Development Environment (RDE) plugin for the Adobe I/O CLI?",
    correctAnswers: [
      "To deploy code directly to an AEM RDE instance without a full pipeline run",
      "Enables fast code deployment to RDE environments, bypassing the Cloud Manager pipeline",
      "Allows developers to push changes to an RDE for rapid iteration without pipeline",
      "Provides CLI commands to install packages directly on an AEM Rapid Development Environment",
    ],
    incorrectAnswers: [
      "To run unit tests locally against a remote AEM instance",
      "To generate AEM project scaffolding from the command line",
      "To trigger Cloud Manager production pipelines from the terminal",
      "To sync local content with the AEM cloud production environment",
      "To analyze code quality using SonarQube from the terminal",
      "To configure OSGi bundles in a running AEM instance",
      "To generate Repo Init scripts for a given project",
      "To manage AEM user accounts from the command line",
    ],
    explanation:
      "The AEM RDE plugin lets developers deploy code (bundles, content packages) directly to an AEM Rapid Development Environment without triggering a Cloud Manager pipeline, enabling fast feedback loops during development.",
  },
  {
    id: "env-024",
    topic: "Adobe I/O CLI",
    question: "How many key plugins are commonly added to the Adobe I/O CLI for AEM development?",
    correctAnswers: [
      "Three plugins",
      "Three key plugins: Cloud Manager, RDE, and Asset Compute",
      "Three — Cloud Manager plugin, AEM RDE plugin, and Asset Compute plugin",
      "Three plugins are commonly used: Cloud Manager, Rapid Development Environment, and Asset Compute",
    ],
    incorrectAnswers: [
      "One plugin",
      "Two plugins",
      "Four plugins",
      "Five plugins",
      "Six plugins",
      "Seven plugins",
      "Eight plugins",
      "No plugins are needed — aio works out of the box for AEM",
    ],
    explanation:
      "Three key plugins are commonly added to the Adobe I/O CLI for AEM development: the Cloud Manager plugin, the AEM Rapid Development Environment (RDE) plugin, and the Asset Compute plugin.",
  },

  // ── FILE SYSTEM ORGANIZATION (8) ─────────────────────────────────────────────

  {
    id: "env-025",
    topic: "File System Organization",
    question: "What is the Adobe-recommended top-level directory for local AEM SDK files?",
    correctAnswers: [
      "~/aem-sdk/",
      "The ~/aem-sdk/ directory",
      "Adobe recommends ~/aem-sdk/ as the top-level directory for AEM SDK files",
      "aem-sdk in your home directory (~)",
    ],
    incorrectAnswers: [
      "~/aem/",
      "~/adobe/",
      "~/workspace/aem/",
      "C:/aem/",
      "~/dev/aem-sdk/",
      "~/Documents/aem/",
      "~/projects/aem-sdk/",
      "~/sdk/aem/",
    ],
    explanation:
      "Adobe recommends organizing local AEM SDK files under `~/aem-sdk/`, with subdirectories for author, publish, and dispatcher. This keeps SDK files separate from your project code.",
  },
  {
    id: "env-026",
    topic: "File System Organization",
    question: "Where should the local Author service files be placed according to the recommended directory structure?",
    correctAnswers: [
      "~/aem-sdk/author/",
      "In the author/ subdirectory under ~/aem-sdk/",
      "~/aem-sdk/author/ — the recommended location for local Author files",
      "The author/ folder inside ~/aem-sdk/",
    ],
    incorrectAnswers: [
      "~/aem-sdk/publish/",
      "~/aem-sdk/dispatcher/",
      "~/aem/author/",
      "~/code/author/",
      "~/aem-sdk/runtime/author/",
      "~/author/",
      "~/aem-sdk/instances/author/",
      "~/aem-sdk/crx-quickstart/",
    ],
    explanation:
      "The recommended structure places Author service files in `~/aem-sdk/author/`, Publish files in `~/aem-sdk/publish/`, and Dispatcher files in `~/aem-sdk/dispatcher/`.",
  },
  {
    id: "env-027",
    topic: "File System Organization",
    question: "Where should the AEM Maven project code be placed in the recommended file system structure?",
    correctAnswers: [
      "~/code/<project-name>/",
      "In ~/code/<project-name>/ — separate from the SDK directory",
      "The AEM Maven project lives in ~/code/<project-name>/",
      "Under ~/code/, in a folder named after the project",
    ],
    incorrectAnswers: [
      "~/aem-sdk/<project-name>/",
      "~/aem-sdk/author/code/",
      "~/projects/<project-name>/",
      "Inside ~/aem-sdk/dispatcher/",
      "~/workspace/<project-name>/",
      "~/aem/<project-name>/",
      "~/Documents/code/<project-name>/",
      "~/aem-sdk/code/",
    ],
    explanation:
      "The AEM Maven project (your custom code) should be placed in `~/code/<project-name>/`, kept separate from the SDK runtime directories in `~/aem-sdk/`. This separation makes it easy to manage multiple SDK versions.",
  },
  {
    id: "env-028",
    topic: "File System Organization",
    question: "What is the main benefit of following the recommended ~/aem-sdk/ directory structure?",
    correctAnswers: [
      "It keeps everything organized and makes it easy to manage multiple SDK versions",
      "Organized structure allows managing multiple AEM SDK versions without conflicts",
      "Keeping SDK files in ~/aem-sdk/ makes version management and organization straightforward",
      "Easy organization and ability to switch between SDK versions without confusion",
    ],
    incorrectAnswers: [
      "It improves AEM startup performance by keeping files on the fastest disk path",
      "Adobe's tools require this exact path to detect the SDK automatically",
      "It prevents file permission errors on macOS and Linux",
      "It allows the aio CLI to discover the local instances automatically",
      "Required for the Docker-based Dispatcher to mount volumes correctly",
      "Ensures Maven can find the QuickStart Jar during the build",
      "Required by Cloud Manager for local pipeline simulation",
      "It enables automatic replication between local Author and Publish",
    ],
    explanation:
      "The recommended structure keeps everything organized and makes it easy to manage multiple SDK versions. When Adobe releases a new SDK, you can keep old and new versions side by side without conflicts.",
  },
  {
    id: "env-029",
    topic: "File System Organization",
    question: "What does the tilde (~) represent in file paths on Windows systems?",
    correctAnswers: [
      "%HOMEPATH%",
      "On Windows, ~ maps to %HOMEPATH%",
      "The tilde (~) maps to the %HOMEPATH% environment variable on Windows",
      "~ is equivalent to %HOMEPATH% — the user's home directory on Windows",
    ],
    incorrectAnswers: [
      "C:\\Users\\",
      "%USERPROFILE%\\Desktop\\",
      "%APPDATA%",
      "%LOCALAPPDATA%",
      "C:\\Program Files\\",
      "%SYSTEMROOT%",
      "%TEMP%",
      "%ALLUSERSPROFILE%",
    ],
    explanation:
      "On Windows, the tilde (~) in file paths maps to `%HOMEPATH%`, which typically resolves to `C:\\Users\\<username>`. So `~/aem-sdk/` becomes `C:\\Users\\<username>\\aem-sdk\\`.",
  },
  {
    id: "env-030",
    topic: "File System Organization",
    question: "Which subdirectory under ~/aem-sdk/ is designated for Local Dispatcher Tools?",
    correctAnswers: [
      "~/aem-sdk/dispatcher/",
      "The dispatcher/ subdirectory",
      "dispatcher/ — one of the three subdirectories under ~/aem-sdk/",
      "~/aem-sdk/dispatcher/ holds the local Dispatcher Tools",
    ],
    incorrectAnswers: [
      "~/aem-sdk/apache/",
      "~/aem-sdk/docker/",
      "~/aem-sdk/proxy/",
      "~/aem-sdk/httpd/",
      "~/aem-sdk/cdn/",
      "~/aem-sdk/cache/",
      "~/aem-sdk/varnish/",
      "~/aem-sdk/nginx/",
    ],
    explanation:
      "The three subdirectories under `~/aem-sdk/` are: `author/` for the local Author service, `publish/` for the local Publish service, and `dispatcher/` for the local Dispatcher Tools.",
  },
  {
    id: "env-031",
    topic: "File System Organization",
    question: "How many subdirectories does the recommended ~/aem-sdk/ structure contain?",
    correctAnswers: [
      "Three subdirectories",
      "Three: author/, publish/, and dispatcher/",
      "~/aem-sdk/ has three subdirectories: author, publish, and dispatcher",
      "Three directories — one each for Author, Publish, and Dispatcher",
    ],
    incorrectAnswers: [
      "Two subdirectories",
      "Four subdirectories",
      "Five subdirectories",
      "One subdirectory",
      "Six subdirectories",
      "Seven subdirectories",
      "Eight subdirectories",
      "No subdirectories — all files go directly in ~/aem-sdk/",
    ],
    explanation:
      "The `~/aem-sdk/` directory contains three subdirectories: `author/` (local Author service), `publish/` (local Publish service), and `dispatcher/` (local Dispatcher Tools).",
  },
  {
    id: "env-032",
    topic: "File System Organization",
    question: "Why is the AEM Maven project kept in ~/code/ rather than inside ~/aem-sdk/?",
    correctAnswers: [
      "To separate your custom code from the runtime SDK files",
      "Keeping code and SDK separate makes it easy to upgrade the SDK without touching your project",
      "~/code/ is for your project; ~/aem-sdk/ is for runtime — separating them avoids confusion",
      "Separation allows managing SDK upgrades independently from the project codebase",
    ],
    incorrectAnswers: [
      "Because Maven requires projects to be in the ~/code/ path to function correctly",
      "Because the QuickStart Jar refuses to start if project code is in the same directory",
      "Because the aio CLI only scans ~/code/ for AEM projects",
      "Because Adobe License terms prohibit placing project code in the SDK directory",
      "Because Git cannot track files inside the ~/aem-sdk/ directory",
      "Because Maven build artifacts would corrupt the QuickStart Jar",
      "Because Docker mounts the ~/code/ directory by default for Dispatcher configuration",
      "Because the Cloud Manager pipeline reads code only from the ~/code/ path",
    ],
    explanation:
      "Keeping project code in `~/code/<project>/` and runtime SDK in `~/aem-sdk/` is a separation of concerns. When Adobe releases a new SDK version, you can update the SDK without touching your code, and vice versa.",
  },

  // ── QUICKSTART JAR (10) ──────────────────────────────────────────────────────

  {
    id: "env-033",
    topic: "QuickStart Jar",
    question: "What is the AEMaaCS SDK QuickStart Jar?",
    correctAnswers: [
      "A self-contained JAR file that runs a full AEM instance locally",
      "A JAR file from the AEMaaCS SDK that runs a complete local AEM instance",
      "The QuickStart Jar is a self-contained file that provides a local AEM runtime",
      "A single executable JAR that launches a full AEM Author or Publish instance locally",
    ],
    incorrectAnswers: [
      "A Docker image that runs AEM inside a container",
      "A Maven plugin that downloads and starts AEM automatically",
      "A lightweight AEM emulator that does not use the JCR",
      "A WAR file deployed on an external Tomcat server",
      "A Node.js-based AEM runtime for local development",
      "A cloud-connected proxy that streams AEM content locally",
      "A ZIP archive containing AEM configuration templates",
      "An npm package that starts a mock AEM server",
    ],
    explanation:
      "The QuickStart Jar is a self-contained JAR file included in the AEMaaCS SDK. It runs a full AEM instance (Author or Publish) locally, providing the same JCR-based runtime as the cloud environment.",
  },
  {
    id: "env-034",
    topic: "QuickStart Jar",
    question: "Where do you download the AEMaaCS SDK QuickStart Jar from?",
    correctAnswers: [
      "Adobe Software Distribution",
      "From Adobe Software Distribution (softwaredistribution.adobe.com)",
      "The QuickStart Jar is downloaded from Adobe Software Distribution",
      "Adobe's Software Distribution portal",
    ],
    incorrectAnswers: [
      "Maven Central Repository",
      "The Adobe Developer Console",
      "The Cloud Manager environment settings",
      "npm registry",
      "GitHub Releases page",
      "Adobe Experience League",
      "The AEM Package Manager of a cloud instance",
      "The Adobe Admin Console",
    ],
    explanation:
      "The AEMaaCS SDK, including the QuickStart Jar, is downloaded from Adobe Software Distribution (softwaredistribution.adobe.com). You need an Adobe ID and appropriate licenses to access it.",
  },
  {
    id: "env-035",
    topic: "QuickStart Jar",
    question: "What is the correct way to start the QuickStart Jar?",
    correctAnswers: [
      "From the command line using: java -jar aem-author-p4502.jar",
      "Run java -jar <filename>.jar from the terminal",
      "Start it from the terminal with the java -jar command",
      "Use the java -jar command in the terminal — never double-click the file",
    ],
    incorrectAnswers: [
      "Double-click the JAR file in the file explorer",
      "Run it with mvn spring-boot:run",
      "Execute it with npm start",
      "Launch it through the Adobe I/O CLI with aio aem:start",
      "Open it through the AEM Package Manager",
      "Start it with the Docker run command",
      "Deploy it to a local Tomcat server",
      "Run it using java -cp aem-author-p4502.jar com.adobe.aem.Main",
    ],
    explanation:
      "The QuickStart Jar must always be started from the command line using `java -jar aem-author-p4502.jar`. Never double-click the JAR file — this can cause it to start incorrectly or corrupt the crx-quickstart folder.",
  },
  {
    id: "env-036",
    topic: "QuickStart Jar",
    question: "Why should you never double-click the QuickStart Jar file?",
    correctAnswers: [
      "It must be started from the command line to ensure it starts correctly",
      "Double-clicking can start it incorrectly and potentially corrupt the crx-quickstart folder",
      "The JAR must be launched via java -jar to ensure the correct startup parameters",
      "Starting via double-click bypasses the correct startup mode and can corrupt the runtime",
    ],
    incorrectAnswers: [
      "Because double-clicking is not supported on macOS",
      "Because Adobe's license terms prohibit it",
      "Because it would run on a random port instead of 4502",
      "Because it would start in read-only mode without write access",
      "Because double-clicking installs it globally instead of locally",
      "Because the JAR file is encrypted and requires a key passed via command line",
      "Because double-clicking would trigger a Cloud Manager deployment",
      "Because Java is not associated with JAR files on most systems",
    ],
    explanation:
      "The QuickStart Jar must be started via `java -jar` from the command line. Double-clicking may start it without proper parameters, potentially starting on a wrong port or in the wrong mode, and can corrupt the `crx-quickstart` folder.",
  },
  {
    id: "env-037",
    topic: "QuickStart Jar",
    question: "On which port does the local AEM Author instance run by default?",
    correctAnswers: [
      "Port 4502",
      "4502 — the default port for local AEM Author",
      "Author runs on port 4502",
      "The default Author port is 4502",
    ],
    incorrectAnswers: [
      "Port 4503",
      "Port 8080",
      "Port 8443",
      "Port 80",
      "Port 443",
      "Port 9090",
      "Port 3000",
      "Port 7402",
    ],
    explanation:
      "The local AEM Author instance runs on port 4502 by default, while the local Publish instance runs on port 4503. These ports are encoded in the QuickStart Jar filename.",
  },
  {
    id: "env-038",
    topic: "QuickStart Jar",
    question: "On which port does the local AEM Publish instance run by default?",
    correctAnswers: [
      "Port 4503",
      "4503 — the default port for local AEM Publish",
      "Publish runs on port 4503",
      "The default Publish port is 4503",
    ],
    incorrectAnswers: [
      "Port 4502",
      "Port 8080",
      "Port 8443",
      "Port 80",
      "Port 443",
      "Port 9090",
      "Port 3000",
      "Port 5503",
    ],
    explanation:
      "The local AEM Publish instance runs on port 4503 by default. Author runs on 4502. These ports are specified in the QuickStart Jar filename and control where AEM listens for requests.",
  },
  {
    id: "env-039",
    topic: "QuickStart Jar",
    question: "What is the crx-quickstart folder created by the QuickStart Jar?",
    correctAnswers: [
      "The runtime folder containing AEM's repository, logs, and configuration",
      "The folder created on first startup that holds the JCR repository, logs, and OSGi bundles",
      "AEM's runtime directory containing the Oak repository, logs, and configuration files",
      "The working directory AEM creates on first start, containing the JCR and all runtime data",
    ],
    incorrectAnswers: [
      "The Maven build output directory",
      "A temporary cache folder that is deleted on each startup",
      "The directory containing the QuickStart Jar and its dependencies",
      "The Dispatcher configuration folder",
      "The Adobe I/O CLI working directory",
      "A log-only folder with no repository data",
      "The OSGi bundle cache located inside the WAR file",
      "The folder where Maven packages are installed",
    ],
    explanation:
      "On first startup, the QuickStart Jar creates a `crx-quickstart` folder in the same directory as the JAR. It contains the Oak JCR repository, logs, OSGi bundle cache, and all AEM runtime data.",
  },
  {
    id: "env-040",
    topic: "QuickStart Jar",
    question: "What must you do if you need to change the tier (Author vs Publish) of a running QuickStart Jar instance?",
    correctAnswers: [
      "Delete the crx-quickstart folder and start fresh",
      "Remove the crx-quickstart directory and restart with the new JAR filename",
      "The tier is locked on first startup — delete crx-quickstart to reset it",
      "Delete crx-quickstart and rename the JAR file before restarting",
    ],
    incorrectAnswers: [
      "Edit the sling.properties file inside crx-quickstart",
      "Run java -jar with the --tier flag to override the current tier",
      "Use the OSGi console to switch the run mode",
      "Change the port number in the JAR filename and restart",
      "Edit the web.xml file inside crx-quickstart/server/",
      "Use the aio CLI to change the tier remotely",
      "Reinstall Java and restart the JAR",
      "Update the OSGi run mode configuration in /system/console",
    ],
    explanation:
      "The tier (author or publish) is locked on the first startup of the QuickStart Jar. To change it, you must delete the `crx-quickstart` folder and restart — AEM will re-read the tier from the JAR filename.",
  },
  {
    id: "env-041",
    topic: "QuickStart Jar",
    question: "Can the same QuickStart Jar run both Author and Publish instances simultaneously?",
    correctAnswers: [
      "Yes, using two separate copies of the JAR in different directories with different filenames",
      "Yes — place two copies in separate directories (author/ and publish/) with the correct filenames",
      "Yes, by running one JAR named for Author and another named for Publish in separate folders",
      "Yes, with two JAR copies in separate directories pointing to different ports",
    ],
    incorrectAnswers: [
      "No — a single QuickStart Jar can only ever be used for one tier",
      "No — you need two different SDK downloads for Author and Publish",
      "Yes, by passing --author and --publish flags to a single JAR invocation",
      "Yes, by running the JAR with the -multi flag",
      "No — running both requires a special multi-tier license from Adobe",
      "Yes, but only in Docker using the multi-container Dispatcher image",
      "No — Author and Publish must run on the same port when local",
      "Yes, but only via the aio CLI with the aem:multi command",
    ],
    explanation:
      "The same QuickStart Jar can run both Author and Publish simultaneously by placing two copies in separate directories (`~/aem-sdk/author/` and `~/aem-sdk/publish/`) with filenames that encode the correct tier and port.",
  },
  {
    id: "env-042",
    topic: "QuickStart Jar",
    question: "What is the AEMaaCS SDK?",
    correctAnswers: [
      "The AEM as a Cloud Service Software Development Kit, which includes the QuickStart Jar and Dispatcher Tools",
      "The SDK provided by Adobe for local AEM as a Cloud Service development, including the QuickStart Jar",
      "Adobe's SDK for AEMaaCS development — provides the local runtime, Dispatcher Tools, and APIs",
      "The Software Development Kit for AEM as a Cloud Service, used to set up local development environments",
    ],
    incorrectAnswers: [
      "A Maven archetype for generating new AEM projects",
      "A set of npm packages for building AEM front-end components",
      "The Cloud Manager API client library",
      "A Docker image that runs the full Adobe Experience Cloud stack",
      "The Adobe I/O CLI and its collection of plugins",
      "A testing framework for AEM OSGi bundles",
      "A browser extension for interacting with AEM Author",
      "A Kubernetes Helm chart for deploying AEM to local clusters",
    ],
    explanation:
      "The AEMaaCS SDK (AEM as a Cloud Service Software Development Kit) is a package provided by Adobe that includes the QuickStart Jar (for local AEM runtime) and the Dispatcher Tools (for local Dispatcher). It is downloaded from Adobe Software Distribution.",
  },

  // ── QUICKSTART JAR NAMING CONVENTION (10) ────────────────────────────────────

  {
    id: "env-043",
    topic: "QuickStart Jar Naming Convention",
    question: "What is the format of the QuickStart Jar filename?",
    correctAnswers: [
      "aem-<tier>_<environment>-p<port>.jar",
      "The format is: aem-<tier>_<environment>-p<port>.jar",
      "aem-[tier]_[environment]-p[port].jar — where tier is author or publish",
      "The naming pattern is aem-<tier>_<environment>-p<port>.jar",
    ],
    incorrectAnswers: [
      "aem-<environment>-<tier>-<port>.jar",
      "aem_<tier>-<port>_<environment>.jar",
      "adobe-aem-<tier>-p<port>.jar",
      "quickstart-<tier>-<environment>-<port>.jar",
      "<tier>-aem-<port>.jar",
      "aem-<port>-<tier>.jar",
      "aem-sdk-<tier>-<environment>.jar",
      "aem-local-<tier>_<port>.jar",
    ],
    explanation:
      "The QuickStart Jar filename follows the format `aem-<tier>_<environment>-p<port>.jar`. The tier (author/publish) and optional environment segment control how AEM starts up. The port after `-p` determines the listening port.",
  },
  {
    id: "env-044",
    topic: "QuickStart Jar Naming Convention",
    question: "What does the filename aem-author-p4502.jar tell AEM when it starts?",
    correctAnswers: [
      "Start as an Author instance in Dev mode on port 4502",
      "Run as Author in development mode listening on port 4502",
      "Author tier, default (dev) environment, port 4502",
      "Launch an Author instance on port 4502 in the default dev run mode",
    ],
    incorrectAnswers: [
      "Start as a Publish instance on port 4502",
      "Start as Author in production mode on port 4502",
      "Start as Author in staging mode on port 4502",
      "Start as a preview tier on port 4502",
      "Start as Author with no port specified",
      "Start as Author on port 4503 in dev mode",
      "Start the Dispatcher on port 4502",
      "Start as Author in preview mode on port 4502",
    ],
    explanation:
      "`aem-author-p4502.jar` tells AEM to start as an Author instance in the default Dev mode, listening on port 4502. No environment segment means the default dev run mode is used.",
  },
  {
    id: "env-045",
    topic: "QuickStart Jar Naming Convention",
    question: "What does the filename aem-publish-p4503.jar tell AEM when it starts?",
    correctAnswers: [
      "Start as a Publish instance in Dev mode on port 4503",
      "Run as Publish in development mode listening on port 4503",
      "Publish tier, default (dev) environment, port 4503",
      "Launch a Publish instance on port 4503 in the default dev run mode",
    ],
    incorrectAnswers: [
      "Start as an Author instance on port 4503",
      "Start as Publish in production mode on port 4503",
      "Start as Publish in staging mode on port 4503",
      "Start as Publish on port 4502",
      "Start the Dispatcher on port 4503",
      "Start as Publish in preview mode on port 4503",
      "Start as Publish with a random port",
      "Start as Author in dev mode on port 4503",
    ],
    explanation:
      "`aem-publish-p4503.jar` tells AEM to start as a Publish instance in the default Dev mode, listening on port 4503.",
  },
  {
    id: "env-046",
    topic: "QuickStart Jar Naming Convention",
    question: "What does the filename aem-author_prod-p4502.jar tell AEM when it starts?",
    correctAnswers: [
      "Start as Author in Production mode on port 4502",
      "Author tier, production environment mode, port 4502",
      "Launch Author in prod run mode on port 4502",
      "Run an Author instance in production mode listening on port 4502",
    ],
    incorrectAnswers: [
      "Start as Author in Dev mode on port 4502",
      "Start as Author in staging mode on port 4502",
      "Start as Publish in production mode on port 4502",
      "Start as Author in preview mode on port 4502",
      "Start the Dispatcher in production mode on port 4502",
      "Start as Author in production mode on port 4503",
      "Deploy to production Cloud Manager from port 4502",
      "Start as Author with production content loaded on port 4502",
    ],
    explanation:
      "`aem-author_prod-p4502.jar` starts AEM as an Author instance in Production mode on port 4502. The `_prod` segment sets the environment run mode, which activates production-specific OSGi configurations.",
  },
  {
    id: "env-047",
    topic: "QuickStart Jar Naming Convention",
    question: "Which part of the QuickStart Jar filename controls the AEM tier?",
    correctAnswers: [
      "The segment after aem- and before the port (author or publish)",
      "The tier segment: author or publish — the first part after 'aem-'",
      "The tier identifier (author/publish) in the filename",
      "The part immediately after 'aem-' — either 'author' or 'publish'",
    ],
    incorrectAnswers: [
      "The port number after -p",
      "The environment segment after the underscore",
      "The .jar file extension",
      "The 'aem-' prefix itself",
      "The version number embedded in the filename",
      "The SDK build number in the filename",
      "The optional -dev suffix",
      "The numeric portion before the port",
    ],
    explanation:
      "In the filename `aem-<tier>_<environment>-p<port>.jar`, the tier is the segment immediately after `aem-`. It must be either `author` or `publish`, and it is locked on first startup.",
  },
  {
    id: "env-048",
    topic: "QuickStart Jar Naming Convention",
    question: "Which part of the QuickStart Jar filename controls the listening port?",
    correctAnswers: [
      "The number after -p in the filename",
      "The port segment after '-p', e.g. p4502 means port 4502",
      "The -p<port> portion of the filename",
      "The numeric value following the '-p' in the filename",
    ],
    incorrectAnswers: [
      "The tier segment (author or publish)",
      "The environment segment after the underscore",
      "The 'aem-' prefix",
      "The .jar extension",
      "The version number in the filename",
      "The SDK build timestamp",
      "The optional environment suffix",
      "The first four digits of the filename",
    ],
    explanation:
      "The port is specified by the `-p<port>` segment in the filename. For example, `-p4502` means AEM will listen on port 4502. The port can be customized by changing this number in the filename.",
  },
  {
    id: "env-049",
    topic: "QuickStart Jar Naming Convention",
    question: "What is the purpose of the environment segment (e.g., _prod, _stage) in the QuickStart Jar filename?",
    correctAnswers: [
      "It sets the environment run mode, activating environment-specific OSGi configurations",
      "Controls which OSGi configurations are loaded based on the environment run mode",
      "Sets the AEM environment run mode (prod, stage, dev) for OSGi config scoping",
      "Determines which environment-specific OSGi configurations are activated at startup",
    ],
    incorrectAnswers: [
      "It sets the AEM admin password for that environment",
      "It controls which content is replicated to the Publish instance",
      "It determines which Cloud Manager environment to sync with",
      "It specifies the Java heap size for that environment",
      "It tells AEM which database to connect to",
      "It controls the log level for that startup mode",
      "It determines whether SonarQube analysis is run on startup",
      "It sets the CDN caching policy for the environment",
    ],
    explanation:
      "The environment segment (e.g., `_prod`, `_stage`, `_dev`) sets the AEM run mode for that environment. This allows environment-specific OSGi configurations to be loaded — for example, production configs that use different endpoints than dev configs.",
  },
  {
    id: "env-050",
    topic: "QuickStart Jar Naming Convention",
    question: "What happens to the tier when the QuickStart Jar is started for the first time?",
    correctAnswers: [
      "The tier is locked and cannot be changed without deleting crx-quickstart",
      "AEM locks the tier (author or publish) on first startup — it cannot be changed later without resetting",
      "The tier becomes permanent — stored in crx-quickstart — until the folder is deleted",
      "The tier is persisted in the crx-quickstart folder and cannot be changed without deleting it",
    ],
    incorrectAnswers: [
      "The tier can be changed at any time by editing sling.properties",
      "The tier can be switched using the OSGi console without restarting",
      "The tier is re-read from the filename on every startup",
      "The tier is stored in a configuration file that can be freely edited",
      "The tier can be changed by renaming the JAR and restarting",
      "The tier is a suggestion only — AEM uses the run mode configuration to override it",
      "The tier can be changed by running java -jar with the --tier flag",
      "The tier resets to author on every startup unless explicitly set",
    ],
    explanation:
      "The tier (author or publish) is locked on first startup and stored in the `crx-quickstart` folder. If you rename the JAR file to change the tier and restart, AEM will use the stored tier — not the new filename. You must delete `crx-quickstart` to reset it.",
  },
  {
    id: "env-051",
    topic: "QuickStart Jar Naming Convention",
    question: "What are the two valid values for the tier segment of the QuickStart Jar filename?",
    correctAnswers: [
      "author and publish",
      "The two valid tier values are author and publish",
      "Either 'author' or 'publish'",
      "author (for Author service) or publish (for Publish service)",
    ],
    incorrectAnswers: [
      "author and preview",
      "master and replica",
      "primary and secondary",
      "author and dispatcher",
      "editor and delivery",
      "author and cdn",
      "cms and delivery",
      "author and stage",
    ],
    explanation:
      "The tier segment in the QuickStart Jar filename accepts exactly two values: `author` (for the Author service on port 4502) and `publish` (for the Publish service on port 4503).",
  },
  {
    id: "env-052",
    topic: "QuickStart Jar Naming Convention",
    question: "If you rename the QuickStart Jar from aem-author-p4502.jar to aem-publish-p4503.jar after the first startup, what happens?",
    correctAnswers: [
      "AEM still starts as Author because the tier is locked in crx-quickstart",
      "Nothing changes — the tier is already locked in crx-quickstart from the first startup",
      "AEM ignores the filename change and starts with the previously locked tier",
      "The instance still runs as Author — the crx-quickstart folder stores the original tier",
    ],
    incorrectAnswers: [
      "AEM switches to Publish mode because it reads the filename on every startup",
      "AEM starts in an error state because the filename and crx-quickstart conflict",
      "AEM starts as Publish but migrates the Author content automatically",
      "The rename causes AEM to create a new crx-quickstart folder for Publish",
      "AEM prompts the user to choose between the old and new tier",
      "AEM starts normally but logs a warning about the tier mismatch",
      "AEM creates two instances — one Author and one Publish",
      "The JAR refuses to start until crx-quickstart is updated to match",
    ],
    explanation:
      "After the first startup, the tier is locked in the `crx-quickstart` folder. Renaming the JAR does not change the tier — AEM reads the stored tier from `crx-quickstart`. To change the tier, you must delete `crx-quickstart` and start fresh.",
  },

  // ── CONTENT DISTRIBUTION (8) ─────────────────────────────────────────────────

  {
    id: "env-053",
    topic: "Content Distribution",
    question: "How does content move from Author to Publish in a production AEMaaCS environment?",
    correctAnswers: [
      "Through the Adobe Pipeline — a cloud-only microservice",
      "Via the Adobe Pipeline microservice, which is available only in the cloud",
      "Content moves through the Adobe Pipeline, a cloud microservice not available locally",
      "The Adobe Pipeline — a cloud-only content distribution service",
    ],
    incorrectAnswers: [
      "Through legacy Replication Agents configured on Author",
      "Via a direct JDBC connection between Author and Publish databases",
      "Through the Dispatcher cache flush mechanism",
      "Via Cloud Manager content sync pipelines",
      "Through the JCR observation API on the Publish node",
      "Via the Adobe I/O CLI content push command",
      "Through the AEM Package Manager replication feature",
      "Via a shared Oak repository cluster",
    ],
    explanation:
      "In production AEMaaCS, content moves from Author to Publish through the Adobe Pipeline — a cloud-only microservice. This mechanism is not available for local development, so Replication Agents are used instead.",
  },
  {
    id: "env-054",
    topic: "Content Distribution",
    question: "How do you simulate content distribution from Author to Publish in a local AEM environment?",
    correctAnswers: [
      "Using legacy Replication Agents configured on the local Author service",
      "By enabling legacy Replication Agents on the Author instance",
      "Configure a legacy Replication Agent on Author pointing to localhost:4503",
      "Enable and configure Replication Agents on Author to replicate to the local Publish",
    ],
    incorrectAnswers: [
      "By installing the Adobe Pipeline Docker image locally",
      "By enabling the Adobe Sling Content Distribution bundle in OSGi",
      "By using the aio CLI with the aem:replicate command",
      "By configuring a shared Oak datastore between Author and Publish",
      "Through the Dispatcher hot-reload mechanism",
      "By copying content packages manually using Package Manager",
      "By using the Cloud Manager content copy feature",
      "By enabling JCR observation on the local Publish instance",
    ],
    explanation:
      "Locally, you simulate the Adobe Pipeline by enabling legacy Replication Agents on the Author instance. These agents push content to the local Publish instance when you activate pages or assets.",
  },
  {
    id: "env-055",
    topic: "Content Distribution",
    question: "Where in AEM do you go to enable the local Replication Agent for content distribution?",
    correctAnswers: [
      "/etc/replication/agents.author.html",
      "Navigate to /etc/replication/agents.author.html on the Author instance",
      "The Author replication agents page at /etc/replication/agents.author.html",
      "Go to /etc/replication/agents.author.html on the local Author service",
    ],
    incorrectAnswers: [
      "/system/console/replication",
      "/libs/cq/replication/content/replication.html",
      "/content/replication/agents",
      "/apps/replication/agents.html",
      "/conf/global/replication",
      "/system/console/bundles",
      "/crx/de/index.jsp#/etc/replication",
      "/libs/granite/core/content/login.html",
    ],
    explanation:
      "To configure Replication Agents, navigate to `/etc/replication/agents.author.html` on the local Author instance. From there you can open the Default Agent, enable it, and set the transport URI.",
  },
  {
    id: "env-056",
    topic: "Content Distribution",
    question: "Which Replication Agent do you enable for local content distribution?",
    correctAnswers: [
      "The Default Agent",
      "The Default Agent on the Author instance",
      "Default Agent — the standard replication agent for Author-to-Publish",
      "The 'Default Agent' configured in /etc/replication/agents.author.html",
    ],
    incorrectAnswers: [
      "The Flush Agent",
      "The Dispatcher Flush Agent",
      "The Reverse Replication Agent",
      "The Static Agent",
      "The Publish Agent",
      "The Content Distribution Agent",
      "The Adobe Pipeline Agent",
      "The Remote Agent",
    ],
    explanation:
      "The Default Agent is the standard Replication Agent for pushing content from Author to Publish. For local development, you enable the Default Agent and point its transport URI to the local Publish instance.",
  },
  {
    id: "env-057",
    topic: "Content Distribution",
    question: "What transport URI should the Default Replication Agent use to point to the local Publish instance?",
    correctAnswers: [
      "localhost:4503",
      "http://localhost:4503/bin/receive?sling:authRequestLogin=1",
      "The transport URI should point to localhost:4503",
      "Point the transport URI to localhost on port 4503",
    ],
    incorrectAnswers: [
      "localhost:4502",
      "localhost:8080",
      "localhost:4503/crx/repository",
      "localhost:80/publish",
      "publish.local:4503",
      "0.0.0.0:4503",
      "127.0.0.1:4502",
      "localhost:4503/system/console",
    ],
    explanation:
      "The Default Replication Agent's transport URI should point to the local Publish instance at `localhost:4503`. The full URI is typically `http://localhost:4503/bin/receive?sling:authRequestLogin=1`.",
  },
  {
    id: "env-058",
    topic: "Content Distribution",
    question: "Why can't the Adobe Pipeline be used for local AEM development?",
    correctAnswers: [
      "Because the Adobe Pipeline is a cloud-only microservice not available locally",
      "The Adobe Pipeline runs only in the cloud and has no local equivalent",
      "It is a cloud microservice — there is no local version of the Adobe Pipeline",
      "The Adobe Pipeline is exclusive to cloud environments and cannot run on localhost",
    ],
    incorrectAnswers: [
      "Because the Adobe Pipeline requires a paid license not included in the SDK",
      "Because the Adobe Pipeline only works with Java 11, not Java 21",
      "Because the Pipeline is blocked by the local Dispatcher",
      "Because it requires a live internet connection that development machines don't have",
      "Because the Adobe Pipeline only supports the Author tier",
      "Because Cloud Manager must approve all content before the Pipeline can run",
      "Because the Pipeline is tied to a specific Cloud Manager environment ID",
      "Because Adobe deprecated the Pipeline in January 2025",
    ],
    explanation:
      "The Adobe Pipeline is a cloud-only microservice. It exists only in AEMaaCS cloud environments and has no local equivalent. Developers use legacy Replication Agents to simulate the same Author-to-Publish flow locally.",
  },
  {
    id: "env-059",
    topic: "Content Distribution",
    question: "What happens after you enable the Default Replication Agent and point it to localhost:4503?",
    correctAnswers: [
      "Content published on Author will replicate to the local Publish instance",
      "Anything you publish on the local Author will be replicated to the local Publish",
      "Page activations on Author are sent to the local Publish via the configured agent",
      "Publishing content on Author triggers replication to the local Publish at localhost:4503",
    ],
    incorrectAnswers: [
      "Content is automatically synced between Author and Publish without manual activation",
      "The Dispatcher cache is invalidated automatically on every page save",
      "Cloud Manager is notified of the content change and deploys it",
      "The Adobe Pipeline takes over and handles distribution from that point",
      "AEM begins a scheduled batch sync of all content to Publish",
      "Content packages are exported to the ~/aem-sdk/publish/ directory",
      "The OSGi Event Admin broadcasts content change events to Publish",
      "The Author instance restarts and imports all content from Publish",
    ],
    explanation:
      "Once the Default Replication Agent is enabled and pointed to `localhost:4503`, any page or asset you activate (publish) on the local Author instance will be replicated to the local Publish instance — simulating the cloud content distribution behavior.",
  },
  {
    id: "env-060",
    topic: "Content Distribution",
    question: "Why are Replication Agents described as 'legacy' in the context of AEMaaCS?",
    correctAnswers: [
      "Because AEMaaCS uses the Adobe Pipeline for content distribution, making Replication Agents a legacy approach",
      "Replication Agents are the old Author-to-Publish mechanism — AEMaaCS replaced them with the Adobe Pipeline",
      "In cloud environments, the Adobe Pipeline handles content distribution, so Replication Agents are considered legacy",
      "The Adobe Pipeline is the modern replacement for Replication Agents in AEMaaCS",
    ],
    incorrectAnswers: [
      "Because Replication Agents were deprecated and removed from AEMaaCS entirely",
      "Because Replication Agents only work with AEM 6.x on-premise, not AEMaaCS",
      "Because Adobe renamed them to Content Sync Agents in AEMaaCS",
      "Because Replication Agents require Java 8, which AEMaaCS no longer supports",
      "Because they were replaced by OSGi Event Admin-based distribution",
      "Because they conflict with the Cloud Manager pipeline when used in production",
      "Because Replication Agents use HTTP/1.0, which AEMaaCS has deprecated",
      "Because Adobe removed the Default Agent from the AEMaaCS SDK",
    ],
    explanation:
      "In AEMaaCS, content distribution between Author and Publish is handled by the Adobe Pipeline (a cloud microservice). Replication Agents are considered 'legacy' because they were the original mechanism from older AEM versions. They are still available locally but are not the primary cloud mechanism.",
  },

  // ── LOCAL DISPATCHER (8) ─────────────────────────────────────────────────────

  {
    id: "env-061",
    topic: "Local Dispatcher",
    question: "What technology do the local Dispatcher Tools use to run the Dispatcher?",
    correctAnswers: [
      "Docker",
      "The local Dispatcher Tools are Docker-based",
      "Docker — the Dispatcher runs inside a Docker container locally",
      "Docker containers — the Dispatcher Tools run an Apache HTTP Server with Dispatcher inside Docker",
    ],
    incorrectAnswers: [
      "Node.js",
      "Java",
      "VirtualBox",
      "Kubernetes",
      "Vagrant",
      "WSL (Windows Subsystem for Linux)",
      "Apache Tomcat",
      "Maven",
    ],
    explanation:
      "The local Dispatcher Tools are Docker-based. They run an Apache HTTP Server with the AEM Dispatcher module inside a Docker container, providing the same caching and filtering behavior as the cloud Dispatcher.",
  },
  {
    id: "env-062",
    topic: "Local Dispatcher",
    question: "What script do you use to start the local Dispatcher with hot-reload enabled?",
    correctAnswers: [
      "./bin/docker_run_hot_reload.sh",
      "The script ./bin/docker_run_hot_reload.sh starts the local Dispatcher with hot-reload",
      "./bin/docker_run_hot_reload.sh ./src localhost:4503 8080",
      "docker_run_hot_reload.sh — the preferred script for starting the local Dispatcher",
    ],
    incorrectAnswers: [
      "./bin/docker_run.sh",
      "./bin/start_dispatcher.sh",
      "./bin/dispatcher_start.sh",
      "docker run aem-dispatcher",
      "./bin/validate.sh",
      "docker-compose up dispatcher",
      "./bin/aem_dispatcher_start.sh",
      "mvn dispatcher:start",
    ],
    explanation:
      "The local Dispatcher is started with `./bin/docker_run_hot_reload.sh ./src localhost:4503 8080`. The hot-reload variant is preferred because it automatically reloads Dispatcher configuration when files change.",
  },
  {
    id: "env-063",
    topic: "Local Dispatcher",
    question: "What is the full command to start the local Dispatcher with hot-reload?",
    correctAnswers: [
      "./bin/docker_run_hot_reload.sh ./src localhost:4503 8080",
      "Run: ./bin/docker_run_hot_reload.sh ./src localhost:4503 8080",
      "The command is: ./bin/docker_run_hot_reload.sh ./src localhost:4503 8080",
      "./bin/docker_run_hot_reload.sh ./src localhost:4503 8080 — connects Dispatcher to Publish on 4503 and exposes on 8080",
    ],
    incorrectAnswers: [
      "./bin/docker_run_hot_reload.sh ./src localhost:4502 8080",
      "./bin/docker_run_hot_reload.sh ./src localhost:4503 80",
      "./bin/docker_run.sh ./src localhost:4503 8080",
      "./bin/docker_run_hot_reload.sh ./conf localhost:4503 8080",
      "./bin/docker_run_hot_reload.sh ./src localhost:4503 443",
      "./bin/dispatcher_start.sh ./src localhost:4503 8080",
      "docker run -p 8080:80 aem-dispatcher ./src localhost:4503",
      "./bin/docker_run_hot_reload.sh localhost:4503 ./src 8080",
    ],
    explanation:
      "The full command is `./bin/docker_run_hot_reload.sh ./src localhost:4503 8080`. This connects the Dispatcher to the local Publish instance at `localhost:4503` and exposes the Dispatcher on port 8080.",
  },
  {
    id: "env-064",
    topic: "Local Dispatcher",
    question: "On which port does the local Dispatcher expose the site when using the standard startup command?",
    correctAnswers: [
      "Port 8080",
      "8080 — the local Dispatcher is exposed on port 8080",
      "The Dispatcher is exposed on port 8080 by default",
      "Port 8080, as specified in the startup command",
    ],
    incorrectAnswers: [
      "Port 80",
      "Port 443",
      "Port 4503",
      "Port 4502",
      "Port 8443",
      "Port 3000",
      "Port 9090",
      "Port 8081",
    ],
    explanation:
      "The startup command `./bin/docker_run_hot_reload.sh ./src localhost:4503 8080` exposes the local Dispatcher on port 8080. This is where you access the site as it would appear through the Dispatcher (cached, filtered requests).",
  },
  {
    id: "env-065",
    topic: "Local Dispatcher",
    question: "Which local Publish port does the Dispatcher connect to in the standard startup command?",
    correctAnswers: [
      "localhost:4503",
      "Port 4503 — the local Publish instance",
      "The Dispatcher connects to the local Publish service at localhost:4503",
      "4503, the default local Publish port",
    ],
    incorrectAnswers: [
      "localhost:4502",
      "localhost:8080",
      "localhost:80",
      "localhost:443",
      "localhost:4504",
      "localhost:9090",
      "localhost:3000",
      "localhost:8443",
    ],
    explanation:
      "In the command `./bin/docker_run_hot_reload.sh ./src localhost:4503 8080`, `localhost:4503` is the address of the local Publish instance. The Dispatcher proxies requests from port 8080 to Publish at 4503.",
  },
  {
    id: "env-066",
    topic: "Local Dispatcher",
    question: "What is the advantage of using docker_run_hot_reload.sh over docker_run.sh?",
    correctAnswers: [
      "It reloads Dispatcher configuration automatically when files change",
      "Hot-reload watches for configuration file changes and reloads without restarting the container",
      "Automatically picks up Dispatcher configuration changes without requiring a container restart",
      "It enables automatic configuration reloading on file change, speeding up development",
    ],
    incorrectAnswers: [
      "It starts the Dispatcher faster than docker_run.sh",
      "It supports HTTPS while docker_run.sh only supports HTTP",
      "It connects to both Author and Publish simultaneously",
      "It includes the Dispatcher validator built into the startup",
      "It automatically deploys changes to Cloud Manager",
      "It allows the Dispatcher to run without Docker",
      "It supports live AEM content preview without Publish",
      "It has better memory performance than docker_run.sh",
    ],
    explanation:
      "The `docker_run_hot_reload.sh` script monitors the Dispatcher configuration directory and reloads the configuration automatically when files change. This avoids needing to restart the Docker container during development.",
  },
  {
    id: "env-067",
    topic: "Local Dispatcher",
    question: "What tool do you use to validate local Dispatcher configuration before deploying?",
    correctAnswers: [
      "./bin/validate ./src",
      "The validator script: ./bin/validate ./src",
      "Run ./bin/validate ./src to check Apache and Dispatcher configuration for errors",
      "./bin/validate — the Dispatcher Tools validator script",
    ],
    incorrectAnswers: [
      "./bin/docker_run_hot_reload.sh --validate",
      "mvn dispatcher:validate",
      "aio dispatcher:validate",
      "./bin/dispatcher_check.sh",
      "docker run dispatcher --validate ./src",
      "./bin/test_dispatcher.sh ./src",
      "apachectl configtest",
      "./bin/docker_run.sh --check-config",
    ],
    explanation:
      "The command `./bin/validate ./src` checks your Apache and Dispatcher configuration for errors before deploying. It helps catch configuration mistakes locally, before they reach Cloud Manager.",
  },
  {
    id: "env-068",
    topic: "Local Dispatcher",
    question: "What does the local Dispatcher run inside?",
    correctAnswers: [
      "A Docker container running Apache HTTP Server with the Dispatcher module",
      "A Docker container with Apache HTTP Server and the AEM Dispatcher module",
      "Docker — it runs Apache HTTP Server with the Dispatcher module inside a container",
      "A containerized Apache HTTP Server with the Dispatcher module installed",
    ],
    incorrectAnswers: [
      "A Node.js process that emulates Dispatcher caching behavior",
      "A Java process embedded inside the AEM QuickStart Jar",
      "A Kubernetes pod running the Dispatcher microservice",
      "A separate JVM process managed by Maven",
      "An Nginx-based container that proxies requests to Publish",
      "A Varnish cache instance running as a Docker container",
      "A Windows IIS instance configured as an AEM Dispatcher",
      "A Python-based mock Dispatcher for local development",
    ],
    explanation:
      "The local Dispatcher runs inside a Docker container. The container runs Apache HTTP Server with the AEM Dispatcher module installed — the same combination used in cloud environments.",
  },

  // ── MAVEN PROJECT STRUCTURE (10) ─────────────────────────────────────────────

  {
    id: "env-069",
    topic: "Maven Project Structure",
    question: "What are the two types of repository areas in AEM's Maven project structure?",
    correctAnswers: [
      "Immutable and Mutable",
      "Immutable (cannot change at runtime) and Mutable (can change at runtime)",
      "Immutable areas (/apps, /libs) and Mutable areas (/content, /conf, /var)",
      "Two types: Immutable (code) and Mutable (content)",
    ],
    incorrectAnswers: [
      "Static and Dynamic",
      "Locked and Unlocked",
      "Read-only and Read-write",
      "Author and Publish",
      "Private and Public",
      "Core and Extension",
      "Versioned and Unversioned",
      "Deployed and Runtime",
    ],
    explanation:
      "AEM enforces a clear separation between two repository areas: Immutable (cannot be changed at runtime — `/apps`, `/libs`) and Mutable (can be changed at runtime — `/content`, `/conf`, `/var`, `/etc`). This separation is critical for AEMaaCS.",
  },
  {
    id: "env-070",
    topic: "Maven Project Structure",
    question: "Which of the following repository paths is considered Immutable in AEM?",
    correctAnswers: [
      "/apps",
      "/apps — the path for custom components, scripts, and clientlibs",
      "/apps is an immutable path that cannot be changed at runtime",
      "/apps and /libs are both immutable repository areas",
    ],
    incorrectAnswers: [
      "/content",
      "/conf",
      "/var",
      "/etc",
      "/home",
      "/tmp",
      "/oak:index",
      "/settings",
    ],
    explanation:
      "`/apps` is an immutable repository path. It stores custom components, HTL scripts, clientlibs, and OSGi bundle overlays. It cannot be changed at runtime — all changes must go through the deployment pipeline.",
  },
  {
    id: "env-071",
    topic: "Maven Project Structure",
    question: "Which of the following repository paths is considered Mutable in AEM?",
    correctAnswers: [
      "/content",
      "/content — a mutable area that can be changed at runtime",
      "/content is a mutable repository path that holds site content",
      "/content, /conf, /var, and /etc are all mutable paths",
    ],
    incorrectAnswers: [
      "/apps",
      "/libs",
      "/apps/myproject",
      "/libs/granite",
      "/apps/components",
      "/apps/clientlibs",
      "/apps/settings",
      "/libs/wcm",
    ],
    explanation:
      "`/content` is a mutable repository path. It holds site content (pages, assets) that can be created, edited, and published at runtime by authors. Other mutable paths include `/conf`, `/var`, and `/etc`.",
  },
  {
    id: "env-072",
    topic: "Maven Project Structure",
    question: "What does /libs contain in the AEM repository?",
    correctAnswers: [
      "AEM platform code — the out-of-the-box components and libraries provided by Adobe",
      "/libs contains AEM's built-in platform code that should never be modified",
      "The AEM platform's own components, scripts, and libraries — do not touch this path",
      "Adobe's out-of-the-box AEM platform code — immutable and must not be modified",
    ],
    incorrectAnswers: [
      "Your custom components and scripts",
      "Third-party npm libraries for front-end development",
      "Java dependency JARs downloaded by Maven",
      "Content pages and assets created by authors",
      "OSGi configurations for your custom bundles",
      "Dispatcher filter and cache configuration files",
      "Content Fragment models created in the AEM UI",
      "User-generated content from the Publish tier",
    ],
    explanation:
      "`/libs` contains AEM's own platform code — the out-of-the-box components, templates, and libraries provided by Adobe. This path is immutable and should never be modified directly. Custom overlays go in `/apps`.",
  },
  {
    id: "env-073",
    topic: "Maven Project Structure",
    question: "Why should you never modify content directly under /libs?",
    correctAnswers: [
      "Because /libs is AEM's platform code — modifications are overwritten on AEM upgrades",
      "Adobe's platform code in /libs is overwritten on AEM updates — changes there are lost",
      "/libs is immutable platform code; changes there would be wiped out by AEM upgrades",
      "Modifications to /libs are overwritten when Adobe upgrades AEM — use /apps overlays instead",
    ],
    incorrectAnswers: [
      "Because /libs requires a special admin password that developers don't have",
      "Because /libs is encrypted and read-only at the file system level",
      "Because CRXDE Lite blocks write access to /libs for all users",
      "Because changes to /libs trigger a Cloud Manager pipeline automatically",
      "Because /libs is backed up hourly and changes would be reverted",
      "Because content in /libs is replicated only to Publish, not stored in Author",
      "Because Maven cannot package content from /libs into a content package",
      "Because the OSGi bundle console prevents modifications to /libs",
    ],
    explanation:
      "You should never modify `/libs` because it contains Adobe's platform code. On AEM upgrades, `/libs` is overwritten, so any direct modifications would be lost. The correct approach is to create overlays in `/apps` — AEM's Sling resource merger picks up `/apps` first.",
  },
  {
    id: "env-074",
    topic: "Maven Project Structure",
    question: "Which Maven module deploys code to the /apps path?",
    correctAnswers: [
      "ui.apps",
      "The ui.apps module deploys to /apps",
      "ui.apps — the code package that deploys to the /apps repository path",
      "The ui.apps Maven module, which is a code package targeting /apps",
    ],
    incorrectAnswers: [
      "core",
      "ui.content",
      "ui.config",
      "all",
      "ui.frontend",
      "ui.tests",
      "dispatcher",
      "it.tests",
    ],
    explanation:
      "`ui.apps` is the Maven module that packages and deploys content to the `/apps` path. It contains HTL components, scripts, clientlibs, and OSGi component configurations. It is a code package of type `application`.",
  },
  {
    id: "env-075",
    topic: "Maven Project Structure",
    question: "Which Maven module deploys content to /content and /conf?",
    correctAnswers: [
      "ui.content",
      "The ui.content module deploys to /content and /conf",
      "ui.content — the content package that deploys mutable content",
      "The ui.content Maven module targets /content and /conf",
    ],
    incorrectAnswers: [
      "core",
      "ui.apps",
      "ui.config",
      "all",
      "ui.frontend",
      "ui.tests",
      "dispatcher",
      "it.tests",
    ],
    explanation:
      "`ui.content` is the Maven module that deploys mutable content to `/content` and `/conf`. It contains page templates, initial content, editable template policies, and Content Fragment models. It is a content package of type `content`.",
  },
  {
    id: "env-076",
    topic: "Maven Project Structure",
    question: "What type of content goes in the /conf repository path?",
    correctAnswers: [
      "Configuration content — editable templates, CF models, and similar configurations",
      "/conf holds configurations like editable template policies and Content Fragment models",
      "Mutable configuration content such as editable templates and global settings",
      "Site-level configuration content including editable templates and CF models",
    ],
    incorrectAnswers: [
      "Custom Java OSGi bundles",
      "HTL scripts and components",
      "Apache Dispatcher filter rules",
      "Maven build artifacts",
      "Clientlib JavaScript and CSS",
      "Error logs and system diagnostics",
      "DAM assets and binary files",
      "User account data",
    ],
    explanation:
      "`/conf` is a mutable repository path used for configuration content — editable templates, Content Fragment models, global configurations, and similar settings that can be modified at runtime by administrators.",
  },
  {
    id: "env-077",
    topic: "Maven Project Structure",
    question: "What repository path is used for custom components, HTL scripts, and clientlibs?",
    correctAnswers: [
      "/apps",
      "/apps — the immutable path for custom code assets",
      "Custom components and scripts go under /apps",
      "/apps holds custom components, HTL scripts, clientlibs, and OSGi configurations",
    ],
    incorrectAnswers: [
      "/libs",
      "/content",
      "/conf",
      "/var",
      "/etc",
      "/home",
      "/bin",
      "/system",
    ],
    explanation:
      "Custom components, HTL scripts, clientlibs, and other code assets are stored under `/apps`. This is the immutable area for developer-created code. `/libs` is reserved for Adobe's out-of-the-box platform code.",
  },
  {
    id: "env-078",
    topic: "Maven Project Structure",
    question: "What is the key principle behind AEM's Immutable vs Mutable repository separation?",
    correctAnswers: [
      "Code lives in immutable paths (/apps); content lives in mutable paths (/content, /conf)",
      "Separating code (immutable) from content (mutable) ensures clean deployments and upgrades",
      "Immutable areas hold deployed code; mutable areas hold runtime-editable content",
      "Code and configuration that comes from deployment (immutable) is separated from content changed at runtime (mutable)",
    ],
    incorrectAnswers: [
      "Immutable paths are for Author; mutable paths are for Publish",
      "Immutable paths require admin access; mutable paths are accessible to all users",
      "Immutable paths are replicated by the Adobe Pipeline; mutable paths use Replication Agents",
      "Immutable paths are cached by the Dispatcher; mutable paths are always fetched live",
      "Immutable paths are deployed by ui.apps; mutable paths are deployed by the core bundle",
      "Immutable paths store binaries; mutable paths store structured content nodes",
      "Immutable paths are version-controlled; mutable paths are not tracked by Git",
      "Immutable paths run on Author; mutable paths run on Publish",
    ],
    explanation:
      "The core principle is separating code (immutable, deployed via pipeline) from content (mutable, changeable at runtime). Immutable paths like `/apps` hold developer code that changes only through deployments. Mutable paths like `/content` hold content that authors can create and edit at runtime.",
  },

  // ── MAVEN MODULES & PACKAGE TYPES (10) ───────────────────────────────────────

  {
    id: "env-079",
    topic: "Maven Modules & Package Types",
    question: "Which Maven module contains the Java OSGi bundle (services, models, servlets)?",
    correctAnswers: [
      "core",
      "The core module contains the Java OSGi bundle",
      "core — the Maven module that compiles into an OSGi bundle with Java classes",
      "The core module holds Java services, Sling models, and OSGi servlets",
    ],
    incorrectAnswers: [
      "ui.apps",
      "ui.content",
      "ui.config",
      "all",
      "ui.frontend",
      "it.tests",
      "dispatcher",
      "ui.tests",
    ],
    explanation:
      "The `core` module is the Java Maven module that compiles into an OSGi bundle (`.jar`). It contains Java code: OSGi services, Sling Models, servlets, workflow steps, and scheduled jobs.",
  },
  {
    id: "env-080",
    topic: "Maven Modules & Package Types",
    question: "What is the package type of the ui.apps module?",
    correctAnswers: [
      "application",
      "ui.apps has the package type 'application'",
      "The package type for ui.apps is application — a code package",
      "application — the package type assigned to ui.apps (code package)",
    ],
    incorrectAnswers: [
      "content",
      "container",
      "mixed",
      "bundle",
      "osgi",
      "code",
      "library",
      "artifact",
    ],
    explanation:
      "The `ui.apps` module has the package type `application`. This designates it as a code package that targets immutable repository paths (`/apps`). AEMaaCS uses the package type to enforce deployment rules.",
  },
  {
    id: "env-081",
    topic: "Maven Modules & Package Types",
    question: "What is the package type of the ui.content module?",
    correctAnswers: [
      "content",
      "ui.content has the package type 'content'",
      "The package type for ui.content is content — a mutable content package",
      "content — the package type assigned to ui.content",
    ],
    incorrectAnswers: [
      "application",
      "container",
      "mixed",
      "bundle",
      "osgi",
      "code",
      "library",
      "artifact",
    ],
    explanation:
      "The `ui.content` module has the package type `content`. This designates it as a content package that targets mutable repository paths (`/content`, `/conf`). It can be modified at runtime by authors.",
  },
  {
    id: "env-082",
    topic: "Maven Modules & Package Types",
    question: "What is the package type of the 'all' module?",
    correctAnswers: [
      "container",
      "The 'all' module has the package type 'container'",
      "container — the package type assigned to the all module",
      "The all module is a container package that embeds all other modules",
    ],
    incorrectAnswers: [
      "application",
      "content",
      "mixed",
      "bundle",
      "aggregate",
      "wrapper",
      "deploy",
      "artifact",
    ],
    explanation:
      "The `all` module has the package type `container`. As a container package, it embeds all other modules (core bundle, ui.apps, ui.content, ui.config) and is the single artifact deployed by Cloud Manager.",
  },
  {
    id: "env-083",
    topic: "Maven Modules & Package Types",
    question: "What is the role of the 'all' module in an AEM Maven project?",
    correctAnswers: [
      "It is the container package and the single deployment artifact used by Cloud Manager",
      "The all module embeds all other modules and is the only package deployed by Cloud Manager",
      "A container package that bundles core, ui.apps, ui.content, and ui.config for Cloud Manager deployment",
      "The single artifact deployed by Cloud Manager — it embeds all other Maven modules",
    ],
    incorrectAnswers: [
      "It runs all unit tests across all Maven modules",
      "It installs all Maven dependencies in the local repository",
      "It generates the project structure using the AEM Archetype",
      "It contains all Java source code for the project",
      "It deploys all content packages to the local AEM Author instance",
      "It aggregates test reports from all modules",
      "It is the parent POM that manages version dependencies",
      "It contains the Dispatcher configuration for all environments",
    ],
    explanation:
      "The `all` module is the container package that embeds every other module (core OSGi bundle, ui.apps, ui.content, ui.config). Cloud Manager deploys only the `all` package — all other packages must have `cloudManagerTarget` set to `none`.",
  },
  {
    id: "env-084",
    topic: "Maven Modules & Package Types",
    question: "What configuration must all packages except 'all' have so Cloud Manager ignores them?",
    correctAnswers: [
      "cloudManagerTarget set to none",
      "Set cloudManagerTarget to none in their package configuration",
      "The cloudManagerTarget property must be set to 'none'",
      "cloudManagerTarget=none — tells Cloud Manager to skip these packages",
    ],
    incorrectAnswers: [
      "skipDeploy set to true",
      "deployTarget set to none",
      "cloudManagerSkip set to true",
      "packageType set to ignored",
      "deploy set to false",
      "cloudManagerExclude set to true",
      "targetEnvironment set to none",
      "pipeline.skip set to true",
    ],
    explanation:
      "All packages except the `all` container package must have `<cloudManagerTarget>none</cloudManagerTarget>` in their package configuration. This tells Cloud Manager to ignore them — only the `all` package is deployed.",
  },
  {
    id: "env-085",
    topic: "Maven Modules & Package Types",
    question: "Which Maven module holds OSGi configurations and Repo Init scripts?",
    correctAnswers: [
      "ui.config",
      "The ui.config module contains OSGi configurations and Repo Init scripts",
      "ui.config — holds OSGi configs and Repo Init run mode scripts",
      "The ui.config Maven module stores OSGi configurations and repository initialization scripts",
    ],
    incorrectAnswers: [
      "core",
      "ui.apps",
      "ui.content",
      "all",
      "ui.frontend",
      "it.tests",
      "dispatcher",
      "ui.tests",
    ],
    explanation:
      "`ui.config` contains OSGi run mode configurations (`.cfg.json` files) and Repo Init scripts. OSGi configs in `ui.config` are scoped by run mode — they activate only for the matching environment (e.g., author, publish, dev, prod).",
  },
  {
    id: "env-086",
    topic: "Maven Modules & Package Types",
    question: "How many key Maven modules does a standard AEM project have?",
    correctAnswers: [
      "Five key modules",
      "Five: core, ui.apps, ui.content, ui.config, and all",
      "A standard AEM project has five key modules: core, ui.apps, ui.content, ui.config, and all",
      "Five — core (Java), ui.apps (code package), ui.content (content), ui.config (OSGi configs), and all (container)",
    ],
    incorrectAnswers: [
      "Three key modules",
      "Four key modules",
      "Six key modules",
      "Seven key modules",
      "Two key modules",
      "Eight key modules",
      "One module — everything goes in a single POM",
      "Ten modules — one per feature area",
    ],
    explanation:
      "A standard AEM project has five key Maven modules: `core` (Java OSGi bundle), `ui.apps` (code package → /apps), `ui.content` (content package → /content, /conf), `ui.config` (OSGi configs + Repo Init), and `all` (container package deployed by Cloud Manager).",
  },
  {
    id: "env-087",
    topic: "Maven Modules & Package Types",
    question: "What does the 'all' package embed?",
    correctAnswers: [
      "All other Maven modules: core bundle, ui.apps, ui.content, ui.config",
      "It embeds the core OSGi bundle, ui.apps, ui.content, and ui.config packages",
      "All other modules — core, ui.apps, ui.content, ui.config — are embedded in the all package",
      "The all package embeds core (bundle), ui.apps, ui.content, ui.config, and optionally ui.frontend",
    ],
    incorrectAnswers: [
      "Only the core OSGi bundle",
      "Only ui.apps and ui.content",
      "Only the Java source code compiled by Maven",
      "Only the Dispatcher configuration",
      "Only content packages (ui.content and ui.config)",
      "Only code packages (core and ui.apps)",
      "Only the Repo Init scripts from ui.config",
      "Only test artifacts from it.tests",
    ],
    explanation:
      "The `all` package embeds all other modules: the `core` OSGi bundle, the `ui.apps` code package, the `ui.content` content package, and the `ui.config` configuration package. When Cloud Manager deploys `all`, it installs everything.",
  },
  {
    id: "env-088",
    topic: "Maven Modules & Package Types",
    question: "Which package type is used for code packages like ui.apps that deploy to immutable paths?",
    correctAnswers: [
      "application",
      "The 'application' package type is used for code packages targeting /apps",
      "Package type 'application' — for immutable code packages like ui.apps",
      "application — the AEMaaCS package type for code deploying to /apps",
    ],
    incorrectAnswers: [
      "content",
      "container",
      "mixed",
      "code",
      "bundle",
      "library",
      "immutable",
      "deploy",
    ],
    explanation:
      "The `application` package type is assigned to code packages like `ui.apps` that deploy to immutable repository paths (`/apps`). AEMaaCS uses the package type to enforce immutability — `application` packages cannot write to mutable paths at runtime.",
  },

  // ── REPO INIT & AEM ARCHETYPE (8) ────────────────────────────────────────────

  {
    id: "env-089",
    topic: "Repo Init & AEM Archetype",
    question: "What is Repo Init in AEM?",
    correctAnswers: [
      "A mechanism that defines JCR structures (folders, users, groups, ACLs) as part of the application",
      "Repo Init defines repository structures declaratively as part of the deployment",
      "A scripting mechanism to create JCR nodes, service users, groups, and ACLs during deployment",
      "Repo Init scripts define the initial JCR structure required by the application at deployment time",
    ],
    incorrectAnswers: [
      "A tool that initializes a new Maven project from a template",
      "A script that imports content from a backup into the JCR",
      "A cloud function that seeds the AEM repository with sample data",
      "An npm package that sets up the AEM front-end development environment",
      "A Docker script that initializes the local Dispatcher configuration",
      "A Maven plugin that downloads the AEM SDK on first build",
      "A Git hook that validates repository structure before each commit",
      "A Cloud Manager step that provisions a new AEM environment",
    ],
    explanation:
      "Repo Init is a mechanism in AEM that lets you define JCR structures — folder hierarchies, service users, groups, and ACL permissions — as part of the application. These definitions are scripts that run early in the deployment lifecycle.",
  },
  {
    id: "env-090",
    topic: "Repo Init & AEM Archetype",
    question: "What types of JCR structures can Repo Init create?",
    correctAnswers: [
      "Folder structures, service users, groups, and ACL permissions",
      "Repo Init can create folders, service users, groups, and define ACLs",
      "Folders, service users, user groups, and access control permissions",
      "JCR folder hierarchies, service users, user groups, and ACL rules",
    ],
    incorrectAnswers: [
      "OSGi component configurations and run mode settings",
      "HTL component scripts and clientlib CSS files",
      "Maven dependency declarations and build profiles",
      "Cloud Manager environment variables and secrets",
      "Content Fragment models and editable templates",
      "Dispatcher caching rules and filter configurations",
      "Java OSGi service implementations and annotations",
      "Sling Model adapters and injectors",
    ],
    explanation:
      "Repo Init scripts can create: folder structures (JCR node hierarchies), service users (system accounts for OSGi services), user groups, and ACL permissions (access control entries). This eliminates manual repository setup steps.",
  },
  {
    id: "env-091",
    topic: "Repo Init & AEM Archetype",
    question: "In which Maven module do Repo Init scripts live?",
    correctAnswers: [
      "ui.config",
      "Repo Init scripts live in the ui.config module",
      "The ui.config Maven module contains Repo Init scripts",
      "ui.config — where OSGi configurations and Repo Init scripts are stored",
    ],
    incorrectAnswers: [
      "core",
      "ui.apps",
      "ui.content",
      "all",
      "ui.frontend",
      "it.tests",
      "dispatcher",
      "ui.tests",
    ],
    explanation:
      "Repo Init scripts live in `ui.config`, the module that holds OSGi run mode configurations. Repo Init scripts are themselves delivered as OSGi configurations and are scoped by run mode.",
  },
  {
    id: "env-092",
    topic: "Repo Init & AEM Archetype",
    question: "When do Repo Init scripts run during the AEM deployment lifecycle?",
    correctAnswers: [
      "Early in the deployment lifecycle, before any application code runs",
      "Before application code — Repo Init scripts run in the early deployment phase",
      "During the startup phase, before OSGi bundles activate",
      "Early in deployment, before application bundles are activated, to set up the JCR foundation",
    ],
    incorrectAnswers: [
      "After all OSGi bundles have fully activated",
      "Only on the first deployment — they are skipped on subsequent deployments",
      "At author startup but after Publish has already started",
      "During the Cloud Manager build phase, not at runtime",
      "Only when triggered manually via the Package Manager",
      "After the Dispatcher flushes its cache",
      "After authors have published their first content",
      "During the Maven build, before the JAR is compiled",
    ],
    explanation:
      "Repo Init scripts run early in the deployment lifecycle, before application code (OSGi bundles and services) starts. This ensures the required JCR structures (service user accounts, folder hierarchies, ACLs) exist before the application needs them.",
  },
  {
    id: "env-093",
    topic: "Repo Init & AEM Archetype",
    question: "What is the AEM Project Archetype?",
    correctAnswers: [
      "A Maven archetype that generates a standard AEM project structure with best practices",
      "A Maven archetype for bootstrapping new AEM projects with the correct module structure",
      "Adobe's Maven archetype that scaffolds a full AEM Maven project following best practices",
      "A Maven template that generates the standard AEM project structure (core, ui.apps, ui.content, ui.config, all)",
    ],
    incorrectAnswers: [
      "A Docker image that contains a pre-configured AEM development environment",
      "An npm package that initializes AEM front-end projects",
      "A Cloud Manager pipeline template for new AEM environments",
      "A Git repository template for AEM projects on GitHub",
      "The Adobe I/O CLI command for creating new AEM projects",
      "A Yeoman generator for AEM component scaffolding",
      "A Jenkins pipeline library for AEM CI/CD",
      "An Eclipse plugin that creates AEM project wizards",
    ],
    explanation:
      "The AEM Project Archetype is a Maven archetype provided by Adobe. Running it generates a complete, correctly-structured AEM Maven project with all standard modules (`core`, `ui.apps`, `ui.content`, `ui.config`, `all`, `ui.frontend`, `dispatcher`, and test modules) pre-configured with best practices.",
  },
  {
    id: "env-094",
    topic: "Repo Init & AEM Archetype",
    question: "What is the current version of the AEM Project Archetype mentioned in the certification content?",
    correctAnswers: [
      "Version 56",
      "Archetype version 56",
      "The current AEM Project Archetype version is 56",
      "AEM Archetype 56 — the version referenced in the certification material",
    ],
    incorrectAnswers: [
      "Version 40",
      "Version 48",
      "Version 50",
      "Version 52",
      "Version 60",
      "Version 64",
      "Version 36",
      "Version 45",
    ],
    explanation:
      "The AEM Project Archetype version referenced in the certification content is version 56. Adobe regularly updates the archetype — always use the latest version for new projects to get current best practices.",
  },
  {
    id: "env-095",
    topic: "Repo Init & AEM Archetype",
    question: "What modules does the AEM Archetype generate when you scaffold a new project?",
    correctAnswers: [
      "all, core, ui.apps, ui.content, ui.config, ui.frontend, tests, and dispatcher configuration",
      "Generates: all (container), core (Java), ui.apps, ui.content, ui.config, ui.frontend, and dispatcher config",
      "A full project with core, ui.apps, ui.content, ui.config, all, ui.frontend, and dispatcher modules",
      "All standard modules: core, ui.apps, ui.content, ui.config, all, ui.frontend, tests, and dispatcher",
    ],
    incorrectAnswers: [
      "Only core and ui.apps — other modules must be added manually",
      "Only the POM files — source code must be written from scratch",
      "Only the all container package and the core bundle",
      "Only the dispatcher and ui.frontend modules",
      "A single Maven module with everything inside",
      "Only core, ui.apps, and all — content modules are optional",
      "Only Java source files — no content package structure is generated",
      "A Spring Boot project that mimics AEM's module structure",
    ],
    explanation:
      "The AEM Archetype generates a complete project including: `all` (container), `core` (Java OSGi), `ui.apps` (code package), `ui.content` (content package), `ui.config` (OSGi configs + Repo Init), `ui.frontend` (front-end build), test modules, and Dispatcher configuration.",
  },
  {
    id: "env-096",
    topic: "Repo Init & AEM Archetype",
    question: "What does 'run mode scoping' mean for Repo Init scripts?",
    correctAnswers: [
      "Repo Init scripts can be scoped to specific run modes so they only activate in the matching environment",
      "Scripts in ui.config can be run-mode-scoped, activating only for author, publish, dev, or prod",
      "Run mode scoping means a Repo Init script is only applied in the AEM environment matching its run mode",
      "Repo Init scripts are scoped by run mode — a prod-scoped script only runs in production environments",
    ],
    incorrectAnswers: [
      "Run mode scoping limits Repo Init to running once per deployment, not repeatedly",
      "It means Repo Init scripts run in parallel across all run modes simultaneously",
      "Scoping prevents Repo Init scripts from running on the Publish tier",
      "It means Repo Init only applies to the Author instance, never Publish",
      "Run mode scoping restricts which Maven modules can include Repo Init scripts",
      "It means only admin users can trigger Repo Init on specific run modes",
      "Scoping prevents Repo Init from running after the first successful deployment",
      "Run mode scoping is a Cloud Manager concept that does not apply to Repo Init",
    ],
    explanation:
      "Repo Init scripts in `ui.config` can be placed in run mode-specific directories (e.g., `config.author/`, `config.publish/`, `config.prod/`). AEM activates only the scripts matching the current run mode — e.g., a script in `config.author/` only runs on Author instances.",
  },

  // ── TROUBLESHOOTING (8) ──────────────────────────────────────────────────────

  {
    id: "env-097",
    topic: "Troubleshooting",
    question: "What is the first troubleshooting step when something is not working in a local AEM environment?",
    correctAnswers: [
      "Check Package Manager to verify the package was actually installed",
      "Start by checking Package Manager — was the package installed successfully?",
      "First check Package Manager at /crx/packmgr to confirm package installation",
      "Verify package installation in Package Manager before anything else",
    ],
    incorrectAnswers: [
      "Restart the QuickStart Jar immediately",
      "Check the error.log first",
      "Open CRXDE Lite and look for the missing node",
      "Check the Bundle Console for inactive bundles",
      "Run the Dispatcher validator tool",
      "Delete crx-quickstart and start fresh",
      "Check the clientlib debug page",
      "Run mvn clean install to rebuild the project",
    ],
    explanation:
      "The first troubleshooting step is to check Package Manager (`http://localhost:4502/crx/packmgr`) to confirm the package was actually installed. Many issues arise from packages that failed to install silently.",
  },
  {
    id: "env-098",
    topic: "Troubleshooting",
    question: "What is the URL of the AEM Package Manager on a local Author instance?",
    correctAnswers: [
      "http://localhost:4502/crx/packmgr",
      "localhost:4502/crx/packmgr",
      "The Package Manager is at http://localhost:4502/crx/packmgr",
      "/crx/packmgr on port 4502",
    ],
    incorrectAnswers: [
      "http://localhost:4502/crx/de",
      "http://localhost:4502/system/console/bundles",
      "http://localhost:4502/system/console/packages",
      "http://localhost:4502/crx/package-manager",
      "http://localhost:4502/libs/granite/core/content/login.html",
      "http://localhost:4503/crx/packmgr",
      "http://localhost:4502/crx/packmgr/list.jsp",
      "http://localhost:4502/content/dam/packagemanager",
    ],
    explanation:
      "The Package Manager is accessed at `http://localhost:4502/crx/packmgr` on a local Author instance. It shows all installed packages and their status, letting you verify that a deployment succeeded.",
  },
  {
    id: "env-099",
    topic: "Troubleshooting",
    question: "What AEM tool do you use to verify that a node exists in the JCR and has the correct content?",
    correctAnswers: [
      "CRXDE Lite",
      "CRXDE Lite at /crx/de — lets you browse the JCR node tree",
      "Use CRXDE Lite (http://localhost:4502/crx/de) to inspect JCR nodes",
      "CRXDE Lite — the browser-based JCR explorer at /crx/de",
    ],
    incorrectAnswers: [
      "Package Manager",
      "Bundle Console",
      "CRXDE Pro",
      "Oak Explorer",
      "AEM Sites admin",
      "Content Tree",
      "Sling Query Console",
      "OakPAL",
    ],
    explanation:
      "CRXDE Lite (accessed at `http://localhost:4502/crx/de`) is the browser-based JCR explorer. It lets you browse the full repository node tree, check node properties, and verify that deployed content landed in the correct paths.",
  },
  {
    id: "env-100",
    topic: "Troubleshooting",
    question: "What is the URL of CRXDE Lite on a local Author instance?",
    correctAnswers: [
      "http://localhost:4502/crx/de",
      "localhost:4502/crx/de",
      "The CRXDE Lite URL is http://localhost:4502/crx/de",
      "/crx/de on port 4502",
    ],
    incorrectAnswers: [
      "http://localhost:4502/crx/packmgr",
      "http://localhost:4502/system/console/bundles",
      "http://localhost:4502/crx/explorer",
      "http://localhost:4502/crxde",
      "http://localhost:4502/crx/de/index.jsp",
      "http://localhost:4503/crx/de",
      "http://localhost:4502/apps/crxde",
      "http://localhost:4502/libs/crxde",
    ],
    explanation:
      "CRXDE Lite is accessed at `http://localhost:4502/crx/de` on a local Author instance. It provides a browser-based interface for exploring and editing the JCR repository.",
  },
  {
    id: "env-101",
    topic: "Troubleshooting",
    question: "What AEM console do you use to check whether an OSGi bundle is in Active state?",
    correctAnswers: [
      "The Bundle Console at /system/console/bundles",
      "OSGi Bundle Console — http://localhost:4502/system/console/bundles",
      "Check the Bundle Console at /system/console/bundles to see bundle states",
      "The Felix Bundle Console at http://localhost:4502/system/console/bundles",
    ],
    incorrectAnswers: [
      "CRXDE Lite at /crx/de",
      "Package Manager at /crx/packmgr",
      "AEM Health Check at /system/console/healthcheck",
      "OSGi Components Console at /system/console/components",
      "Sling Log Support at /system/console/slinglog",
      "OSGi Configuration at /system/console/configMgr",
      "AEM Sites admin at /sites.html",
      "CRX Explorer at /crx/explorer",
    ],
    explanation:
      "The OSGi Bundle Console at `http://localhost:4502/system/console/bundles` shows all installed OSGi bundles and their states. An `Active` state means the bundle is running correctly. `Installed` or `Resolved` states usually indicate a missing dependency.",
  },
  {
    id: "env-102",
    topic: "Troubleshooting",
    question: "Where is the AEM error.log file located?",
    correctAnswers: [
      "crx-quickstart/logs/error.log",
      "In crx-quickstart/logs/error.log",
      "The error log is at crx-quickstart/logs/error.log",
      "~/aem-sdk/author/crx-quickstart/logs/error.log",
    ],
    incorrectAnswers: [
      "crx-quickstart/error.log",
      "logs/error.log",
      "~/aem-sdk/logs/error.log",
      "crx-quickstart/repository/logs/error.log",
      "/var/log/aem/error.log",
      "crx-quickstart/server/logs/error.log",
      "target/logs/error.log",
      "crx-quickstart/bundles/logs/error.log",
    ],
    explanation:
      "The `error.log` file is located at `crx-quickstart/logs/error.log` — inside the `crx-quickstart` folder created by the QuickStart Jar. AEM logs all errors, warnings, and info messages here. It is the primary log for troubleshooting.",
  },
  {
    id: "env-103",
    topic: "Troubleshooting",
    question: "Which AEM URL provides clientlib debug tools for front-end troubleshooting?",
    correctAnswers: [
      "/libs/granite/ui/content/dumplibs.html",
      "http://localhost:4502/libs/granite/ui/content/dumplibs.html",
      "The clientlib debug page at /libs/granite/ui/content/dumplibs.html",
      "Access dumplibs.html at /libs/granite/ui/content/dumplibs.html for clientlib debugging",
    ],
    incorrectAnswers: [
      "/system/console/bundles",
      "/crx/packmgr",
      "/crx/de",
      "/libs/clientlibs/debug.html",
      "/libs/granite/debug/clientlibs.html",
      "/apps/clientlibs/dumplibs.html",
      "/system/console/clientlibs",
      "/libs/cq/ui/content/dumplibs.html",
    ],
    explanation:
      "The clientlib debug page is at `/libs/granite/ui/content/dumplibs.html`. It shows all registered client libraries, their categories, dependencies, and included files — useful for debugging missing CSS, JavaScript, or incorrect clientlib loading order.",
  },
  {
    id: "env-104",
    topic: "Troubleshooting",
    question: "What are the two most common errors when setting up a local AEM environment?",
    correctAnswers: [
      "Wrong Java version and trying to double-click the QuickStart Jar",
      "Using an incorrect Java version and double-clicking the JAR instead of using java -jar",
      "Incorrect JDK version and not using the command line to start the QuickStart Jar",
      "Running the wrong Java version and starting the JAR by double-clicking it",
    ],
    incorrectAnswers: [
      "Wrong Maven version and missing the aio CLI",
      "Incorrect Node.js version and missing Docker",
      "Wrong SDK version and missing Cloud Manager credentials",
      "Incorrect Dispatcher configuration and missing OSGi bundles",
      "Wrong Git version and missing Adobe Developer Console access",
      "Incorrect crx-quickstart path and missing Repo Init scripts",
      "Missing cloudManagerTarget config and wrong package type",
      "Wrong port number and missing OAuth credentials",
    ],
    explanation:
      "The two most common errors are: (1) using the wrong Java version — AEM requires JDK 21, and (2) double-clicking the QuickStart Jar instead of starting it with `java -jar` from the command line. Both cause the AEM instance to fail to start correctly.",
  },

  // ── LOCAL ENVIRONMENT OVERVIEW (8) ───────────────────────────────────────────

  {
    id: "env-001",
    topic: "Local Environment Overview",
    question: "How many logical parts make up a local AEM development environment?",
    correctAnswers: [
      "Three logical parts",
      "A local AEM environment has three logical parts",
      "Three components: AEM Project, Local AEM Runtime, and Local Dispatcher Runtime",
      "Three parts — the AEM project, the local runtime, and the local dispatcher",
    ],
    incorrectAnswers: [
      "Two logical parts",
      "Four logical parts",
      "Five logical parts",
      "One unified component",
      "Six distinct services",
      "Two runtimes and one project",
      "A single deployable unit",
      "Four components including a database layer",
    ],
    explanation:
      "A local AEM environment has three logical parts: the AEM Project (your custom code and content), the Local AEM Runtime (local Author and Publish services), and the Local Dispatcher Runtime (local Apache HTTP Server with Dispatcher module).",
  },
  {
    id: "env-002",
    topic: "Local Environment Overview",
    question: "Which of the following is NOT one of the three logical parts of a local AEM environment?",
    correctAnswers: [
      "A local database service",
      "A standalone relational database",
      "A separate analytics engine",
      "A dedicated message broker",
    ],
    incorrectAnswers: [
      "The AEM Project",
      "The Local AEM Runtime",
      "The Local Dispatcher Runtime",
      "The local Author and Publish services",
      "Custom code and configuration",
      "Local version of Apache HTTP Server",
      "The Maven project with code and content",
      "The QuickStart Jar runtime",
    ],
    explanation:
      "The three logical parts are: the AEM Project (custom code/config/content), the Local AEM Runtime (local Author and Publish), and the Local Dispatcher Runtime. AEM does not require a separate relational database — it uses the JCR (Oak repository).",
  },
  {
    id: "env-003",
    topic: "Local Environment Overview",
    question: "What is the primary goal of setting up a local AEM development environment?",
    correctAnswers: [
      "To develop, build, and test AEM code on your own machine before sending it to the cloud",
      "To validate everything locally before it goes through Cloud Manager",
      "To run and test AEM code locally before deploying to the cloud pipeline",
      "To have a local runtime for developing and testing AEM projects prior to Cloud Manager deployment",
    ],
    incorrectAnswers: [
      "To replace Cloud Manager with a local CI/CD pipeline",
      "To host production content for end users",
      "To bypass code quality gates during development",
      "To synchronize content directly from production to your machine",
      "To run integration tests against the live Adobe pipeline",
      "To generate deployment artifacts without Maven",
      "To train machine learning models on AEM content",
      "To monitor AEM cloud environments from a local dashboard",
    ],
    explanation:
      "The goal of local AEM setup is to develop, build, and test AEM code on your own machine before sending anything to the cloud. It allows you to validate everything locally before it goes through Cloud Manager.",
  },
  {
    id: "env-004",
    topic: "Local Environment Overview",
    question: "What does the 'Local AEM Runtime' component represent in a local development environment?",
    correctAnswers: [
      "A local version of the Author and Publish services",
      "Local instances of AEM Author and Publish running on your machine",
      "The QuickStart Jar running locally as Author and/or Publish",
      "Local AEM Author and Publish instances for development and testing",
    ],
    incorrectAnswers: [
      "The Maven build tool running locally",
      "A local version of Cloud Manager",
      "The Dispatcher module running inside Docker",
      "The Adobe I/O CLI installed on the machine",
      "A local replica of the Adobe CDN",
      "The OSGi container running independently",
      "A local version of the Adobe Developer Console",
      "The JCR repository backup service",
    ],
    explanation:
      "The Local AEM Runtime is a local version of the Author and Publish services, typically provided by the QuickStart Jar from the AEMaaCS SDK.",
  },
  {
    id: "env-005",
    topic: "Local Environment Overview",
    question: "What does the 'AEM Project' component represent in the three-part local environment?",
    correctAnswers: [
      "Your custom code, configuration, and content",
      "The Maven project containing custom components, OSGi bundles, and content packages",
      "Custom code, configuration, and content for the AEM implementation",
      "The developer's AEM Maven project with application code and content",
    ],
    incorrectAnswers: [
      "The QuickStart Jar downloaded from Adobe",
      "The Docker container running the Dispatcher",
      "The Adobe I/O CLI and its plugins",
      "The AEMaaCS SDK itself",
      "The Cloud Manager pipeline configuration",
      "The Apache HTTP Server configuration",
      "The Adobe Developer Console project",
      "The local Publish instance running on port 4503",
    ],
    explanation:
      "The AEM Project is your custom code, configuration, and content — the Maven project you develop, which contains OSGi bundles, HTL components, content packages, and OSGi configurations.",
  },
  {
    id: "env-006",
    topic: "Local Environment Overview",
    question: "How is a local AEM environment best described in relation to the cloud environment?",
    correctAnswers: [
      "A miniature version of what runs in the cloud, all on your laptop",
      "A local replica of the cloud Author, Publish, and Dispatcher services",
      "A self-contained local copy of the cloud AEM stack for development purposes",
      "A mini cloud environment running locally to validate code before deployment",
    ],
    incorrectAnswers: [
      "An exact clone of the production environment with live data",
      "A simplified version that omits the Dispatcher layer",
      "A cloud-connected sandbox with read-only access to production",
      "A Docker Compose stack that mirrors all Adobe cloud microservices",
      "A virtual machine provided by Adobe for certified developers",
      "A browser-based emulator of AEM running in Adobe's data center",
      "A reduced runtime that skips OSGi and uses plain Java",
      "A CI server environment used only for automated testing",
    ],
    explanation:
      "A local AEM environment is best described as a miniature version of what runs in the cloud, all on your laptop. It includes local Author, Publish, and Dispatcher — the same three tiers present in the cloud environment.",
  },
  {
    id: "env-007",
    topic: "Local Environment Overview",
    question: "What is the Local Dispatcher Runtime in a local AEM development environment?",
    correctAnswers: [
      "A local version of Apache HTTP Server with the Dispatcher module",
      "A Docker-based local Dispatcher that mirrors the cloud Dispatcher behavior",
      "Local Apache HTTP Server running the AEM Dispatcher module via Docker",
      "A containerized local Dispatcher that proxies requests to the local Publish service",
    ],
    incorrectAnswers: [
      "A local version of the AEM Author service",
      "The Maven Sling plugin that deploys packages locally",
      "A local replica of the Adobe CDN caching layer",
      "The AEM QuickStart Jar configured in publish mode",
      "A Node.js reverse proxy for local front-end development",
      "The Adobe I/O CLI gateway service",
      "A Varnish cache instance running on port 80",
      "The OSGi HTTP whiteboard component inside AEM",
    ],
    explanation:
      "The Local Dispatcher Runtime is a local version of Apache HTTP Server with the Dispatcher module. It runs inside a Docker container using the Dispatcher Tools from the AEMaaCS SDK.",
  },
  {
    id: "env-008",
    topic: "Local Environment Overview",
    question: "Why is it important to validate AEM code locally before sending it through Cloud Manager?",
    correctAnswers: [
      "To catch issues early before they go through the CI/CD pipeline",
      "To avoid consuming Cloud Manager pipeline runs for bugs that could be caught locally",
      "Local validation catches errors before the code quality gates and pipeline execution",
      "To reduce build failures and pipeline costs by testing on your machine first",
    ],
    incorrectAnswers: [
      "Because Cloud Manager does not run any code quality checks",
      "Because local environments use a different JCR implementation than production",
      "Because Adobe requires local approval before any pipeline can be triggered",
      "Because Cloud Manager cannot deploy packages without prior local validation",
      "Because the local environment runs a newer AEM version than the cloud",
      "Because OSGi configurations are ignored by Cloud Manager pipelines",
      "Because Content Fragments cannot be tested in Cloud Manager",
      "Because the Adobe Pipeline microservice does not run in non-production environments",
    ],
    explanation:
      "Validating locally first catches bugs, build errors, and configuration issues before they consume a Cloud Manager pipeline run. It is faster and cheaper to fix problems locally than to wait for a full pipeline cycle.",
  },
]
